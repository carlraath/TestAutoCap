{
  "id": "sql-07-a",
  "assessment": "sql",
  "section": "applied",
  "slot": 7,
  "type": "single",
  "stem": "A tester is reconciling two extracts. The tables `source` and `target` each hold an `id` column.\n\n| source.id |\n|---|\n| 101 |\n| 102 |\n| 103 |\n| 104 |\n\n| target.id |\n|---|\n| 102 |\n| 104 |\n| 105 |\n\nWhich query returns the ids that appear in both tables, giving 102 and 104?",
  "options": [
    { "id": "a", "text": "SELECT id FROM source INTERSECT SELECT id FROM target", "correct": true },
    { "id": "b", "text": "SELECT id FROM source UNION SELECT id FROM target", "correct": false },
    { "id": "c", "text": "SELECT id FROM source EXCEPT SELECT id FROM target", "correct": false },
    { "id": "d", "text": "SELECT id FROM target EXCEPT SELECT id FROM source", "correct": false }
  ],
  "fixedOrder": false,
  "difficulty": "standard",
  "rationale": "INTERSECT returns the rows common to both result sets, which here is 102 and 104, so option a is correct. Option b returns every id from either table, 101 to 105. Option c returns the ids in source that are absent from target, 101 and 103. Option d returns the ids in target that are absent from source, 105. Each query was run against the shown rows and only option a produces 102 and 104.",
  "sourceAnchor": "SQL for Testers - validating data integrity with set operations (INTERSECT)"
}
