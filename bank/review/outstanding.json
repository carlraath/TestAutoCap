{
 "groupA": [
  {
   "item": "ta-01-a",
   "revised": 2,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "The key \"The same 400 checks run in the same way at every release, without the two days of manual effort.\" is the only option that echoes the stem's \"two days\" figure (and one of only two that echo \"400\"), so a test-wise candidate can select it by keyword matching rather than by understanding the purpose of automation.",
      "The stem \"Which outcome is the direct purpose of that automation?\" reads awkwardly because an outcome is not a purpose; the wording is clumsy rather than ambiguous, but plainer phrasing would match the charter's plain-sentence rule."
     ],
     "fix": "Remove the stem-number echo from the key, for example: \"The same checks run in the same way at every release, without the repeated manual execution effort.\" and, for symmetry, drop \"400\" from option b (\"The suite finds defects in areas that its cases were never written to cover.\"). Rephrase the stem's final sentence to \"What does that automation directly achieve?\" or \"Which outcome does the automation directly provide?\". Apply the same two edits to sibling ta-01-b (\"60 rules\", \"half day\") so the slot stays interchangeable."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Major: ta-01-a duplicates its only sibling ta-01-b with cosmetic changes only. The stems share one template (\"A ... team [runs/checks] N ... by hand [before each release/after every load]. The [cases/rules] stay the same ... and the [run/check] takes ... The team automates the [suite/checks]. Which outcome is the direct purpose of that automation?\"), and the four options map one-to-one: key a \"The same N checks ... in the same way ... without the ... manual effort\", b \"finds defects ... never written to cover\" / \"in rules other than the 60 that were scripted\", c \"no longer needs to judge which ... matter\", d \"A passing run confirms ... no defects, so no further [testing/checking] is needed\". The rationales are near-verbatim copies. The slot therefore has one item in two costumes, which defeats the paper-variation design in docs/03 and gives no genuine second calibrated item for the slot.",
      "Minor: the three distractors are all over-claims that an experienced professional tester dismisses on sight (\"finds defects in areas that its 400 cases were never written to cover\", \"no longer needs to judge which areas ... carry the most risk\", \"so no further testing is needed\"), so the item sits below the level of a CTAL-TAE graduate and is easier than the docs/04 exemplar for TA slot 5; the sibling shares the same weakness, so interchangeability holds, but the slot as a whole is under-calibrated."
     ],
     "fix": "Rewrite ta-01-a as a different concept-selection item on the same blueprint topic (purpose of automation versus manual testing) rather than a re-skin of ta-01-b. For example, keep a short generic scenario but change the angle: a team lists four goals for a release and the candidate picks the one goal that automating the existing checks directly delivers, with distractors drawn from distinct concepts that remain manual testing's job or are separate objectives (assessing usability of a redesigned screen, exploring an unfamiliar new feature for unexpected behaviour, deciding which features carry most business risk) against a key such as running the same checks after every build to give fast, repeatable feedback. Keep the stem to two or three plain sentences and the option lengths matched to ta-01-b so difficulty and length stay interchangeable, and make each distractor a legitimate testing activity rather than an over-claim so an unsure candidate could genuinely pick it. Apply the same distractor strengthening to ta-01-b so the slot is calibrated as a pair."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The key \"The same checks run identically on every build, with results back sooner than the manual run.\" is the only option that reuses the stem's own vocabulary (\"regression checks\", \"by hand\", release) and is phrased as an execution outcome rather than as one of the \"four goals for testing the next release\" the stem promises, whereas b, c and d are each phrased as a testing activity; a candidate can therefore pick the key from surface cues alone, which makes ta-01-a easier than its sibling ta-01-b, whose four options are in parallel form and none of which echoes its stem."
     ],
     "fix": "Rewrite all four options in the same grammatical form as goals so the key no longer stands out, for example: a \\\"Consistent execution of the release checks, with feedback sooner than the manual run\\\"; b \\\"An assessment of how easy first-time shoppers find the redesigned checkout page to use\\\"; c \\\"Discovery of loyalty scheme behaviour that its written requirements do not describe\\\"; d \\\"Identification of the features carrying the most business risk before testing effort is allocated\\\". Optionally soften the stem cue by replacing \\\"tests each release candidate by hand and is now automating its existing regression checks\\\" with \\\"is automating a set of checks it currently performs manually before each release\\\". Keep the rationale as is."
    }
   ]
  },
  {
   "item": "ta-01-b",
   "revised": 1,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "Option b reads \"The checks find data defects in rules other than the 60 that were scripted.\" and the rationale echoes it with \"defects in other rules are outside its reach\", but defects occur in the loaded data rather than in rules, so the phrase is muddled English even though it does not make b defensible as the key."
     ],
     "fix": "Reword option b to \\\"The checks find data defects that none of the 60 scripted rules describe.\\\" and change the rationale sentence to \\\"b is wrong because an automated check can only report on the rules it was scripted to evaluate; data defects that no scripted rule describes are outside its reach.\\\" No change to the key, which is defensible as written."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Major: ta-01-b is a cosmetic re-skin of ta-01-a rather than an interchangeable alternative - the stem \"A data pipeline team checks 60 reconciliation rules by hand after every daily load. The rules stay the same from one load to the next and the check takes one tester half a day. The team automates the checks. Which outcome is the direct purpose of that automation?\" follows ta-01-a's sentence template word for word with only the nouns and numbers swapped, and all four options (\"The same 60 rules are checked in the same way after every load, without the half day of manual effort.\", \"The checks find data defects in rules other than the 60 that were scripted.\", \"The team no longer needs to judge which rules matter most when the pipeline changes.\", \"A passing run confirms that the loaded data contains no defects, so no further checking is needed.\") map one-to-one onto ta-01-a's options in the same roles with the same phrasing, so a participant who has seen either paper has effectively seen both and the deterministic-variation design in docs/03 gives slot 1 no real variation.",
      "Minor: the key \"The same 60 rules are checked in the same way after every load, without the half day of manual effort.\" is the only option that repeats the stem's own facts (\"60\", \"stay the same\", \"half a day\"), so it can be identified by lexical overlap with the stem without applying the concept, which makes the item easier than the concept-selection level the blueprint intends (the same weakness exists in ta-01-a, so the siblings are equally easy rather than calibrated to the exemplar bar).",
      "Minor: the stem sentence \"Which outcome is the direct purpose of that automation?\" conflates an outcome with a purpose, which reads awkwardly and departs from the plain-sentence rule; a clearer form asks what the automation is intended to achieve."
     ],
     "fix": "Rewrite ta-01-b as a genuinely different scenario on the same blueprint topic (purpose of automation versus manual testing, concept selection) with its own option structure, so that a and b are interchangeable in difficulty but not recognisable as the same item. For example, keep the data-focused setting but change the angle to interpreting what an automated run does and does not establish: \"A team has automated 60 reconciliation checks that run after every daily load. Tonight's run reports that all 60 checks passed. What does that result establish?\" with four options along the lines of: \"The 60 scripted checks held for tonight's load.\" (key); \"Tonight's load contains no data defects.\"; \"The reconciliation rules themselves are correct and complete.\"; \"Manual checking of tonight's load is no longer worthwhile for any purpose.\" Write the key so it states the concept rather than echoing figures from the stem, make at least two distractors also reuse stem figures so lexical overlap does not identify the key, and change the closing sentence to a plain form such as \"What is the automation intended to achieve?\" if the original framing is kept. Update the rationale to explain the key and each distractor for the new scenario and keep the sourceAnchor to ISTQB CTAL-TAE v2 Chapter 1, purpose of automation and automation versus manual testing."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The stem \"Tonight's run reports that all 60 checks passed. What can the team conclude from that result?\" tests the interpretation of a passing run (an automated check reports only on its scripted condition, and a pass does not prove absence of defects), which is a general testing principle every experienced tester already holds rather than the slot 1 blueprint construct \"Purpose of automation vs manual testing - concept selection\"; its sibling ta-01-a asks directly which goal automation delivers versus manual activities, so the two items measure different constructs and ta-01-b is markedly easier for this audience, with the key also identifiable as the most hedged statement without any automation knowledge. [major]",
      "Option d \"The result cannot be relied on until a tester has repeated the 60 checks by hand.\" is not a plausible choice for a genuinely unsure professional, so the item effectively has three live options while its sibling has four, which further lowers its difficulty relative to ta-01-a. [minor]"
     ],
     "fix": "Rewrite ta-01-b so its construct matches ta-01-a (purpose of automation versus manual testing, concept selection). Keep the nightly reporting-load scenario but ask what the team gains by scripting the 60 reconciliation checks instead of a tester running them by hand after each load, with the key being consistent, repeatable execution of the same comparisons with results available soon after every load, and three distractors naming outcomes that automation does not deliver and that remain human activities: judging whether the report layout is readable for its users, exploring the loaded data for problems nobody has specified a rule for, and deciding which totals carry enough business risk to be worth reconciling. If the current pass-interpretation stem is kept instead, replace option d with a plausible confusion such as \\\"The source files themselves contain correct data, because each total was reconciled against them\\\" (reconciliation shows agreement between two sources, not the correctness of either) and add that reasoning to the rationale, but note this still leaves the item off the slot 1 construct."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Major - topic drift from the slot blueprint: the stem \"Tonight's run reports that all 60 checks passed. What can the team conclude from that result?\" tests how to interpret a passing automated run (the limitation that a pass shows only what was scripted, essentially the general 'testing shows presence not absence of defects' principle), whereas the slot 1 blueprint is \"Purpose of automation vs manual testing - concept selection\" and sibling ta-01-a asks which goal automation directly delivers against three manual-testing activities; the two items therefore test different concepts and are not interchangeable in topic, and the 'versus manual' dimension appears in ta-01-b only as distractor d \"repeated the 60 checks by hand\".",
      "Minor - calibration: the key \"Tonight's loaded data met the 60 conditions that the scripted checks evaluate.\" is a near-restatement of the stem's \"all 60 checks passed\", so a candidate with no test automation knowledge can select it by wording alone, which makes ta-01-b easier than ta-01-a, whose key requires recognising consistency and faster feedback as automation's deliverable."
     ],
     "fix": "Rewrite ta-01-b to mirror ta-01-a's construct in the data-pipeline setting so both items test 'what automation delivers versus what stays manual'. For example: stem \"A data team loads a reporting database every night and until now has checked the load by hand each morning. It has automated its 60 reconciliation checks, each comparing a total in the database with the matching total in the source files. The team has set four goals for checking the next load. Which goal does that automation directly deliver?\" Key: \"The same 60 comparisons run identically after every load, with results available before the team starts work.\" Distractors: \"The new dashboard built on the loaded data is judged for whether analysts find its layout clear.\"; \"The new supplier feed is explored for data problems that its interface specification does not describe.\"; \"The loads that carry the most business risk are identified before checking effort is allocated.\" Keep the rationale structure (key delivers repeatable execution and fast feedback; each distractor is human judgement, exploration or risk analysis that automation does not perform) and the existing sourceAnchor. Ensure the key is not a restatement of the stem and that both items remain similar in stem length."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "Minor topic drift from the slot 1 blueprint (\"Purpose of automation vs manual testing - concept selection\"): the stem \"Tonight's run reports that all 60 checks passed. What can the team conclude from that result?\" tests the limitation of an automated pass (what it does and does not establish) rather than the purpose of automation, so it is not fully interchangeable in topic with sibling ta-01-a, which asks \"Which goal does that automation directly deliver?\"; both anchor to the same chapter, so this is a calibration note, not a dispute risk."
     ],
     "fix": "Either accept as-is (the anchor line \"purpose of automation, advantages and limitations, automation versus manual testing\" covers it and the key is beyond argument), or tighten the stem toward purpose so it mirrors ta-01-a, for example: \"A data team loads a reporting database every night and has automated its 60 reconciliation checks, each comparing a total in the database with the matching total in the source files. Which outcome does automating those checks directly deliver?\" with key \"Every load is evaluated against the same 60 conditions with results available soon after the load completes.\" and the current b, c, d reworded as outcomes automation does not deliver (defect-free data, proof that the rules are correct, a manual repeat being required). All structural checks pass: id ta-01-b, assessment ta, section fundamentals, slot 1, type single, 4 options a-d with exactly 1 correct, fixedOrder false, difficulty standard, no visual element, no negative or opinion stem, no banned constructs, no company or person names in participant-facing text, rationale addresses a, b, c and d, sourceAnchor names the TA-1 course and chapter 1 topic from docs/plan/course-syllabi.md, and scripts/validate-bank.ts reports no problems."
    }
   ]
  },
  {
   "item": "ta-02-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "The rationale sentence \"putting API or UI tests above unit tests would describe an inverted pyramid, and placing UI tests above API tests would describe the ice-cream cone anti-pattern\" over-generalises: the sequences API, Unit, UI and Unit, UI, API place API above Unit or UI above API yet are neither the inverted pyramid (UI, API, Unit) nor the ice-cream cone, so the dispute record misnames some wrong permutations even though the preceding general clause rejects them correctly."
     ],
     "fix": "Keep the key and stem unchanged. Rewrite the second half of the rationale so the anti-pattern names are attached only to the permutations they actually describe, for example: \"Any other order places a slower, less isolated layer above a faster, more isolated one, which contradicts the pyramid as taught. The fully reversed order (UI tests, API tests, unit tests) is the inverted pyramid; an order with UI tests most numerous and unit tests in the middle or last is the ice-cream cone anti-pattern; the remaining orders (API, Unit, UI; Unit, UI, API; API, UI, Unit) each swap two adjacent layers and so also contradict the pyramid.\""
    }
   ]
  },
  {
   "item": "ta-02-b",
   "revised": 1,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The stem \"by the typical execution time of one test, from fastest at the top of this list to slowest\" mixes a time quantity (execution time) with speed adjectives (fastest, slowest); it is not disputable but the criterion should be stated in one consistent measure."
     ],
     "fix": "Reword the stem to a single measure, for example: \"Arrange the automated test layers of the test automation pyramid by how quickly one test typically runs, from fastest at the top of this list to slowest.\" Leave elements, key, rationale and sourceAnchor unchanged; the item is otherwise on topic, calibrated to ta-02-a and sanctioned by the docs/04 exemplar."
    }
   ]
  },
  {
   "item": "ta-04-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Item a is markedly easier than sibling b: the stem \"re-runs the existing tests for card and bank-transfer checkout to see that they still pass\" maps to the universally known key \"Regression testing\" with no need to know the term progression testing, whereas b's key \"Progression testing\" is the less familiar ISTQB term, so a candidate is not equally likely to get either sibling right.",
      "The scenario \"A web shop adds a new gift-card payment option\" is a front-end e-commerce framing, while sibling b (\"A payments batch gains a new currency-conversion step\") and the governing principle in docs/04 target a back-end, data-focused automation use case; the pair is not fully interchangeable in framing."
     ],
     "fix": "Raise the discrimination demand of item a to match b by making Confirmation testing a live competitor, and move the scenario to a back-end setting. For example: \"A payments file import gains a new record type. A defect found in the new record type is fixed, and the team re-runs the test that originally found it. The team also re-runs the existing settlement and reconciliation tests for the other record types to see that they still pass. Which classification fits the re-run of the existing settlement and reconciliation tests?\" Key stays Regression testing; keep the same four options; update the rationale so c reads that confirmation testing applies only to the re-run of the test that previously failed, not to the unchanged tests, and keep the same sourceAnchor."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "ta-04-a is markedly busier than ta-04-b: its stem describes three activities (\"writes new tests\", \"re-runs that one test\" after a fix, and \"re-runs the existing settlement and reconciliation tests\") so the candidate must reject two live distractors (progression and confirmation) drawn from the scenario, whereas ta-04-b describes two activities and has only one live distractor (regression), making a candidate less likely to get ta-04-a right than its sibling.",
      "ta-04-a and ta-04-b share the same domain and near-identical phrasing (\"A payments file import gains a new record type\" versus \"A payments batch gains a new currency-conversion step\", and both \"re-runs the existing settlement ... tests to see that they still pass\") with an identical option set, so the two items read as one scenario with the asked-about activity swapped rather than two independent classification scenarios.",
      "ta-04-a's stem is roughly half as long again as ta-04-b's (about 70 words against about 45), so the reading load within the slot is not equalised."
     ],
     "fix": "Rewrite ta-04-a to a different scenario domain and to the same two-activity shape as ta-04-b, so that the two items differ in scenario but match in load, for example: \\\"A customer address form gains an optional postcode-lookup field. The team writes new tests for the lookup and also re-runs the existing address-save and address-display tests to see that they still pass. Which classification fits the re-run of the existing address-save and address-display tests?\\\" Keep the same four options and key (Regression testing), and update the rationale so the confirmation-testing explanation no longer refers to a one-test re-run that the stem no longer mentions (state simply that no test previously failed and no defect fix is described). If the confirmation-testing element is wanted as a live distractor, add an equivalent third activity to ta-04-b as well so both siblings carry two live distractors."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "Difficulty is not equalised with sibling ta-04-b: ta-04-a's stem (69 words, five sentences) adds a third activity, \"One of the new tests finds a defect; after the fix, the team re-runs that one test.\", which makes the Confirmation testing distractor live inside the scenario and requires the candidate to separate regression from confirmation, whereas ta-04-b (46 words, three sentences) presents only two activities and its Confirmation testing distractor has nothing in the scenario to attach to, so ta-04-a is measurably harder and longer to read within the one-minute-per-item budget."
     ],
     "fix": "Equalise the pair in one of two ways. Either remove the sentence \"One of the new tests finds a defect; after the fix, the team re-runs that one test.\" from ta-04-a so both stems present exactly two activities (new tests plus re-run of existing tests) and ask about one of them; or add an equivalent third activity to ta-04-b (for example \"One of the existing settlement tests fails; after the fix, the team re-runs that one test.\") so both scenarios contain new tests, one confirmation re-run and one regression re-run. In either case keep the wording of the two stems parallel and of similar length, and update the rationale for option c to match."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Minor: ta-04-a includes the sentence \"One of the new tests finds a defect; after the fix, the team re-runs that one test.\" which makes confirmation testing a live third decoy and the stem about 74 words, whereas sibling ta-04-b contains no failing test, so its confirmation and smoke options are dead and it reduces to a two-way choice between regression and progression in a 52-word stem; the two items are therefore not equally difficult for an unsure candidate."
     ],
     "fix": "Equalise the pair by giving ta-04-b the same three-activity structure: after \\\"The team writes new tests that exercise the conversion step for the first time\\\" add \\\"One of the new tests finds a defect; after the fix, the team re-runs that one test.\\\" and keep the question targeting the new tests, then extend its rationale for option c to say the confirmation re-run is the single re-tested test, not the new tests as a whole. Alternatively, drop the defect sentence from ta-04-a so both stems present only new tests plus a re-run of existing tests; either way both stems should land within about ten words of each other with the same set of live distractors. No change is needed to ta-04-a's key, options or rationale."
    }
   ]
  },
  {
   "item": "ta-04-b",
   "revised": 1,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "The rationale rejects option d with \"not a set of tests written to exercise a new step in depth\", but the stem only says the tests \"exercise the conversion step for the first time\" and never says anything about depth, so the dispute record argues from a fact that is not on the page."
     ],
     "fix": "In bank/drafts/ta/ta-04-b.json, reword the d clause of the rationale to argue from the stem, for example: \"d is wrong because smoke testing is a quick, broad check that the main functions of a build work at all before further testing; the tests here are written specifically for one newly added step, which is testing of new functionality, not a smoke check of the build.\" The key and options need no change."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Calibration: ta-04-b is easier than ta-04-a because two of its distractors are inert by the stem's own construction, as its rationale admits (\"the stem mentions no earlier failing test and no defect fix\" for Confirmation testing, and Smoke testing does not match \"tests written specifically for one newly added step\"), leaving a two-way choice between Regression and Progression, whereas ta-04-a's scenario contains a defect fix and a single-test re-run so that Regression, Progression and Confirmation are all live choices.",
      "Interchangeability: ta-04-b reuses ta-04-a's scenario skeleton almost verbatim (\"A payments batch gains a new currency-conversion step. The team writes new tests ... and it also re-runs the existing settlement tests to see that they still pass\" against \"A payments file import gains a new record type. The team writes new tests ... re-runs the existing settlement and reconciliation tests ... to see that they still pass\") with the identical option set, so the pair reads as one scenario asked from two ends rather than two independent scenarios of equal weight; ta-04-b's stem is also roughly two thirds the length of ta-04-a's."
     ],
     "fix": "Rewrite ta-04-b in a different setting (for example a nightly reporting extract gaining a new output column) and include, as ta-04-a does, a previously failed test that was fixed and re-run on its own, so that Regression, Progression and Confirmation are all live options; keep the question keyed on the newly written tests (Progression testing) and bring the stem to roughly the same length as ta-04-a, for example: \\\"A nightly reporting extract gains a new output column. The team writes new tests that exercise the new column for the first time. One of the existing extract tests had failed the previous week; after its fix the team re-runs that one test. The team also re-runs the rest of the existing extract tests to see that they still pass. Which classification fits the new tests for the new column?\\\" Update the rationale so each distractor is rejected against something the stem actually contains."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "[minor] Difficulty asymmetry with ta-04-a: the stem \"The team writes new tests that exercise the conversion step for the first time, and it also re-runs the existing settlement tests to see that they still pass\" contains only two activities and no defect-fix re-run, so the distractors \"Confirmation testing\" and \"Smoke testing\" are inert and the item collapses to a two-way choice between regression and progression, whereas ta-04-a's three-activity stem (new tests, a confirmation re-run of one fixed test, and a regression re-run) keeps confirmation as a live distractor; a candidate is not equally likely to get either sibling right.",
      "[minor] Template duplication with ta-04-a: both items use the same payments/settlement framing (\"A payments batch gains a new currency-conversion step\" versus \"A payments file import gains a new record type\"), the same three-part narrative and an identical option list in identical order (Regression, Progression, Confirmation, Smoke); the only substantive difference is which half of the scenario is asked about, which is close to the same scenario with cosmetic changes.",
      "[minor] The question \"Which classification fits the new tests for the conversion step?\" classifies the tests themselves rather than a testing activity, while every option names an activity (\"Progression testing\") and the sibling asks about an activity (\"the re-run of the existing settlement and reconciliation tests\"); the category mismatch is small but avoidable."
     ],
     "fix": "Rewrite ta-04-b in a different back-end scenario and with the same three-activity structure as ta-04-a so that confirmation testing is a live distractor in both siblings, and ask about an activity rather than the tests. For example: \"A nightly reporting job gains a new export column. The team writes new tests that check the new column, and its first run of those tests is the first time the column has been exercised. One of the new tests finds a defect; after the fix, the team re-runs that one test. The team also re-runs the existing totals tests for the other columns to see that they still pass. Which classification fits the first run of the new tests for the new column?\" Key: Progression testing. Keep the same four options, and update the rationale so that confirmation testing is rejected because it applies only to the re-run of the single fixed test, and regression testing is rejected because it applies only to the re-run of the existing totals tests. Keep the sourceAnchor unchanged."
    }
   ]
  },
  {
   "item": "ta-05-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The five options of ta-05-a (\"It runs every release.\", \"The functionality it covers is stable.\", \"Its result needs human visual judgement each run.\", \"Its requirements change frequently.\", \"Its expected outcome is deterministic.\") map one-for-one onto the five options of sibling ta-05-b, so the slot holds one item and one re-skin of it rather than two independently calibrated items sampling the candidacy factor set.",
      "ta-05-a relies on the term of art \"Its expected outcome is deterministic.\" while ta-05-b spells the same factor out as \"Its expected result can be computed exactly from the input data.\", so a candidate who knows the concept but not the word is marginally more likely to miss ta-05-a than its sibling."
     ],
     "fix": "Keep ta-05-a as the binding exemplar. Diversify the sibling instead: in ta-05-b, replace at least one strengthening option and one weakening option with different candidacy factors so the pair is not an option-for-option re-skin, for example a strengthening option \"The check must be run against many combinations of input data.\" and a weakening option \"The check will be run once, after a one-off migration.\" Optionally soften the vocabulary load in ta-05-a by rewording option e to \"Its expected outcome is fixed in advance and does not vary between runs.\" so both items test the concept rather than the word."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Option e \"Its expected outcome is known in advance and does not vary between runs.\" is about twice the length of every other option (4 to 8 words each) and is a key, giving a longest-option cue that ta-05-b does not have, so ta-05-a is marginally easier than its sibling for a test-wise candidate (minor)."
     ],
     "fix": "Shorten option e in bank/drafts/ta/ta-05-a.json to match the length of the other options while keeping the plain-language meaning, for example \"Its expected outcome is the same on every run.\", and update the rationale's wording for e to match; no other change is needed."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "Option e \"Its expected outcome is known in advance and does not vary between runs.\" is 13 words while options a to d are 4 to 8 words, so the conspicuously longest option is a key and gives a length cue to a test-wise candidate."
     ],
     "fix": "Shorten option e to a length in keeping with the others while preserving the deterministic-outcome meaning, for example \"Its expected outcome is the same on every run.\", and update the rationale's paraphrase of option e to match. Everything else in the item (ids, counts, type, section, stem wording, distractors, rationale coverage, sourceAnchor, UK spelling) complies with the charter and structural rules."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Option e \"Its expected outcome is known in advance and does not vary between runs.\" is 13 words against 4 to 8 for the other four options, so the longest option is a correct one and gives a test-wise length cue that its sibling ta-05-b (all options 7 to 10 words) does not give."
     ],
     "fix": "In bank/drafts/ta/ta-05-a.json shorten option e to a length in line with the others, for example \"Its expected outcome is the same on every run.\" (or restore the exemplar's \"Its expected outcome is deterministic.\"), and adjust the first sentence of the rationale to quote the new wording. No other change needed."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "[minor] Option e \"Its expected outcome is known in advance and does not vary between runs.\" is 72 characters against 22 to 49 for the other four options, and it is a key, so its conspicuous length acts as a cue toward the correct set; the sibling ta-05-b keeps all options within 40 to 56 characters."
     ],
     "fix": "Shorten option e to a length comparable with the others while keeping it plain, for example \"Its expected outcome is the same on every run.\" (or revert to the exemplar wording \"Its expected outcome is deterministic.\"), and adjust the first sentence of the rationale to match the new wording."
    }
   ]
  },
  {
   "item": "ta-05-b",
   "revised": 1,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Every option in ta-05-b maps one-to-one onto an option in sibling ta-05-a (\"The check is repeated after every load with the same steps.\" = \"It runs every release.\", \"The rule it checks has not changed in the last year.\" = \"The functionality it covers is stable.\", \"Its expected result can be computed exactly from the input data.\" = \"Its expected outcome is deterministic.\", \"Its pass or fail decision depends on a reviewer's judgement of a chart.\" = \"Its result needs human visual judgement each run.\", \"The rule it checks is redefined in most sprints.\" = \"Its requirements change frequently.\"), and the rationale reuses the sibling's wording nearly verbatim (\"each makes the automated check cheaper to keep and its result trustworthy\", \"has no expected value a script can compare against, so it weakens the case\"), so the item reads as the exemplar with a scenario sentence bolted on rather than an independently authored sibling.",
      "The options in ta-05-b are roughly twice the length of the sibling's (for example \"Its pass or fail decision depends on a reviewer's judgement of a chart.\" against \"Its result needs human visual judgement each run.\") and the stem carries an extra scenario sentence (\"A team is choosing which of the checks it performs on a nightly data pipeline to automate first.\"), so the item is slightly longer to read than its sibling under a 10-minute paper."
     ],
     "fix": "Keep the three strengthening properties and the data-pipeline framing, but replace one mirrored distractor with a different, beyond-argument weakener so the option set is not a point-for-point recast of ta-05-a, for example swap option e for \"The check is needed only once, when the pipeline is first commissioned.\" (a one-off check has no repeat runs to pay back the automation cost). Trim each option to about the sibling's length (for example \"It is repeated after every load.\" / \"Its rule has not changed in a year.\" / \"Its expected result can be computed from the input.\" / \"Its pass or fail decision needs a reviewer to judge a chart.\"), and rewrite the rationale in the item's own terms rather than copying the sibling's sentences."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Option c \"Its expected result can be computed from the input data.\" states the determinism factor indirectly compared with the sibling ta-05-a option \"Its expected outcome is known in advance and does not vary between runs.\", so a candidate must infer that a computable oracle means a deterministic expected outcome, and could read it as requiring the test to re-implement the pipeline's logic, making this item marginally harder than its sibling."
     ],
     "fix": "Reword option c to state the factor directly, for example \"Its expected result is fixed for a given input and does not change between runs.\", and adjust the rationale sentence for c accordingly so it names the deterministic expected outcome rather than computation from input data."
    }
   ]
  },
  {
   "item": "ta-06-b",
   "revised": 1,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "All three distractors lead with a repetition cue (\"Checking every night that\", \"Checking each release that\", \"Checking after every batch run that\") while the key alone leads with \"Checking once that a one-off data migration has completed, after which the migration script is retired\", so a candidate can pick the key by spotting the odd phrasing without reasoning about automation candidacy, which makes ta-06-b marginally easier than ta-06-a, whose key is separated from its distractors by content rather than by a shared surface cue."
     ],
     "fix": "Remove the uniform frequency prefix from at least two distractors so the key is not the only option with a distinct opening, keeping each distractor clearly deterministic and part of an ongoing suite, for example: b \"Checking that each settled payment appears exactly once in the daily settlement file.\", c \"Checking that a payment with an invalid account number is written to the rejects table.\", d \"Checking that the end-of-day totals match the ledger totals after each batch run.\"; and trim the key's triple signposting to a single cue, for example \"Checking that a one-off data migration has completed, after which the migration script is retired.\" Update the rationale wording for b and c accordingly (they remain strong candidates because their expected result is definite and they belong to a suite that runs repeatedly)."
    },
    {
     "lens": "Key defensibility",
     "problems": [
      "Major: option a \"Checking that a one-off migration copied every account record, after which its script is retired.\" presupposes the check is scripted, so a candidate can argue the option describes an already automated check and contradicts a stem asking for \"the weakest candidate for automation\".",
      "Major: \"copied every account record\" describes a full-volume reconciliation that a data-focused tester can defensibly argue is impossible manually and therefore a necessary automation candidate even when run once, and the rationale concedes the tension with \"even though its expected result is definite\", leaving the key open to dispute.",
      "Minor: sibling ta-06-a keys on a check with no deterministic expected result (\"looks appealing to shoppers\") while ta-06-b keys on a frequency cost-benefit trade-off between two candidacy factors, so the two items are not interchangeable in difficulty within the slot."
     ],
     "fix": "Rewrite option a so it is one-off, small enough to be done by hand in minutes, and makes no mention of a script, for example: \"Checking once, before the new scheduler goes live, that its cut-off time is set to 17:00 on the three batch environments.\" Update the rationale to state that the check runs once against a handful of values, so scripting it cannot repay the effort, and remove the concession about the definite expected result. Alternatively align with the sibling by keying on a check with no deterministic expected result, for example \"Checking whether the new settlement report layout is easy for the operations team to read.\", which puts both items at the same difficulty."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "Key option a, \"Checking that a one-off migration copied every account record, after which its script is retired.\", presupposes the check is already scripted, which weakens the premise that it is a poor automation candidate and sits oddly with the stem \"four checks it wants to add to its suite\"; a candidate could argue that verifying every account record is only practical by script and so the option contradicts itself, even though b, c and d remain plainly stronger candidates. (minor)",
      "Difficulty is not fully interchangeable with sibling ta-06-a: the sibling's key (\"looks appealing to shoppers\") is unautomatable because it has no deterministic oracle, whereas ta-06-b's key relies on the subtler run-frequency factor while all four options are deterministic, so ta-06-b is a noticeably harder discrimination for the same slot. (minor)"
     ],
     "fix": "Reword option a so it does not concede a script and does not conflict with the suite framing, for example: \"Checking that a one-off migration copied every account record, a check that will be run once and never repeated.\" Optionally strengthen the run-once signal in the stem, for example \"Which check is the weakest candidate for automation, given how often each will run?\", or bring the key closer to the sibling's clarity by pairing the one-off nature with a second weakening factor so the discrimination is as clear-cut as ta-06-a. Update the rationale wording to match."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Key option a, \"Checking that a one-off migration copied every account record, after which its script is retired.\", is disputable because verifying every account record is infeasible by hand and the option itself states that a script exists, so a competent professional can argue it is a strong candidate for scripting even if it is a poor candidate for the suite; the rationale concedes the weakness with \"even though its expected result is definite\".",
      "The item is markedly harder than sibling ta-06-a: the sibling's key fails candidacy on the most recognisable factor (\"looks appealing to shoppers\", human judgement with no deterministic result) while ta-06-b's key passes determinism and fails only on run frequency, so a candidate is less likely to get ta-06-b right than ta-06-a."
     ],
     "fix": "Rewrite option a so the one-off nature is the sole and unmistakable weakness, remove the implication that a script already exists, and make the check small enough that doing it by hand is obviously trivial, so no reconciliation-scripting counter-argument remains. For example: \"Checking, one time only, that the three reference accounts set up for next week's pilot hold the agreed opening balances.\" Update the rationale to state that the check runs once, covers three values a person can confirm in a minute, and so cannot repay any scripting effort, while the other three checks are deterministic and repeated every day or every batch run. Keep the stem, distractors b to d, and the sourceAnchor unchanged."
    },
    {
     "lens": "Key defensibility",
     "problems": [
      "Key a \"Checking that a one-off migration copied every account record, after which its script is retired.\" is contestable: verifying that every account record was copied is a full-population reconciliation that a competent professional would say cannot practically be done by hand and is routinely scripted despite being one-off, so the volume of the check gives a defensible argument that it is a strong candidate, and the rationale never addresses that argument (major).",
      "The stem \"lists four checks it wants to add to its suite\" is inconsistent with option a's \"after which its script is retired\": a check retired after one run cannot be part of a suite, which both makes the scenario incoherent and signposts the key by being the only option that mentions a script at all (minor).",
      "Option a presupposes the check is already automated (\"its script is retired\"), so it does not describe a candidate for automation in the same terms as b, c and d, which describe checks rather than scripts; this asymmetry is a give-away cue rather than a test of judgement (minor)."
     ],
     "fix": "Rewrite the key so the one-off check is small and trivially manual, removing any volume that would justify tooling, and describe it as a check rather than a script, e.g. option a: \"Checking, once before the new batch scheduler goes live, that its three settlement accounts show the agreed cut-off time.\" Change the stem to remove the suite contradiction, e.g. \"A payments batch team lists four checks it is considering scripting. Which check is the weakest candidate for automation?\" Extend the rationale for a to state explicitly that the check is needed only once and is quick to perform by hand, so the effort of scripting it is never repaid, and that its small size means no scale argument applies. Keep b, c and d as they are."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The key option \"Checking that a one-off migration copied every account record, after which its script is retired.\" is disputable because verifying that every account record was copied is a volume-driven check that a competent professional would script precisely because it is infeasible by hand, and the option text itself concedes a script was written, so a candidate can defensibly argue it is a strong one-time candidate for automation rather than the weakest candidate (major).",
      "The item is markedly harder than its sibling ta-06-a: the sibling's key \"Checking whether the redesigned home page looks appealing to shoppers.\" is a plain human-judgement check with no deterministic result, whereas this item's key asks the candidate to weigh one-off return on effort against a definite, deterministic outcome, so a candidate is not equally likely to get either sibling right (major).",
      "The rationale for option c states it \"belongs to a suite that is run again with every change to the batch\", but the option text \"Checking that a payment with an invalid account number is written to the rejects table.\" says nothing about repetition, unlike options b and d which state \"daily\" and \"after each batch run\", so the rationale relies on a fact not on the page (minor).",
      "The stem \"A payments batch team lists four checks it wants to add to its suite. Which check is the weakest candidate for automation?\" is the sibling's stem with only the team name changed; acceptable structurally, but the two items then differ only in which non-automation reason is hidden in the options, which makes the difficulty gap noted above more visible (minor)."
     ],
     "fix": "Rewrite the key so it is beyond argument and matches the sibling's level, either by using the same human-judgement discriminator as ta-06-a, for example \\\"Checking whether the redesigned settlement report layout is easy for operations staff to read.\\\", or, if the one-off discriminator is kept, remove both the volume cue and the presupposed script, for example \\\"Checking, once only, that a single sample account was carried across correctly before the old system is switched off next week.\\\" Add an explicit repetition cue to option c, for example \\\"... is written to the rejects table on every batch run.\\\", and trim the rationale for c to facts stated in the option. Keep the stem scaffold but consider a small wording change so the two items do not read as the same template."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "Option a, \"Checking that a one-off migration copied every account record, after which its script is retired.\", presupposes the check is already scripted, which sits awkwardly against a stem asking which check is the weakest \"candidate for automation\" and against \"four checks it wants to add to its suite\" (a one-off check retired after one run cannot be added to a suite), giving a candidate a foothold to argue the scenario is internally inconsistent.",
      "The rationale for c asserts \"the check belongs to a suite that is run again with every change to the batch\", a fact that appears nowhere in option c's text (\"Checking that a payment with an invalid account number is written to the rejects table.\"), so the rationale justifies the distractor from an assumption rather than from what the participant can read."
     ],
     "fix": "Rewrite option a so the one-off nature is stated without presupposing a script, for example: \"Checking once, before the old system is switched off, that the migration copied the account records, with no plan to run the check again.\" Adjust the rationale for a accordingly (\"the check runs once and is never repeated, so scripting it cannot repay its cost\"). Trim the rationale for c to what the option states, for example: \"c is wrong because the rejects-table rule gives a definite expected outcome and the batch is run repeatedly, so the check is repeatable and deterministic, a strong candidate.\" No other changes needed; the item otherwise passes every structural and charter check and is interchangeable in topic and difficulty with ta-06-a."
    }
   ]
  },
  {
   "item": "ta-07-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "Option b \"Keep a copy of each identifier in every test so that each test stays self-contained.\" restates the suite's existing design, whereas the stem asks \"Which change to the suite's design reduces the effort\", so b is not a change at all and is a weaker distractor than it could be (the key remains uniquely defensible)."
     ],
     "fix": "Reword option b so it is a genuine change that still preserves the duplication, for example: \"Move each test's identifiers to the top of that test's own file so that each test stays self-contained.\" Update the rationale for b accordingly: it relocates the copies but keeps one copy per test, so a rename still needs 120 edits."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "Option b, \"Keep a copy of each identifier in every test so that each test stays self-contained.\", is the existing design the stem has already blamed for the 120 edits, and the stem asks for a \"change\", so it is not a plausible wrong answer an unsure candidate would pick; it is eliminated by the stem's own wording (charter: distractors must be plausible, never filler).",
      "Option c, \"Add more tests that use each identifier so that a rename is noticed sooner.\", answers a detection question rather than the effort question the stem asks, and proposing to add tests to reduce editing effort is close to absurd, so it is a weak distractor (charter: distractors must be plausible wrong answers a genuinely unsure person might pick)."
     ],
     "fix": "Replace the two weak distractors with plausible design changes that still leave 120 edits per rename. For example, b: \"Store each test's identifiers in a separate data file kept alongside that test, one file per test.\" (sounds like a data-driven improvement but keeps 120 copies), and c: \"Add a retry to each element lookup so that a test tries again before it fails.\" (a plausible robustness change that addresses flakiness, not maintenance). Update the rationale to explain why each is wrong: b keeps one copy per test so a rename still means 120 edits; c changes how a lookup fails, not how many places hold the identifier. Apply the same treatment to option b in sibling ta-07-b so the slot stays calibrated."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "ta-07-a duplicates its sibling ta-07-b too closely: the stem \"A suite of 300 automated UI tests ... each test holds its own copy ... Which change to the suite's design reduces the effort of the next such rename?\" is the same template as ta-07-b's \"A suite of 200 automated API tests ... written out in full inside every test ... Which change to the suite's design reduces the effort of the next such change?\", option b (\"Keep a copy of each identifier in every test so that each test stays self-contained.\") and option d (\"Re-record the affected tests with a capture and replay tool after each rename.\") mirror ta-07-b's b and d word for word apart from the nouns, and the rationale is the same paragraph with 120 substituted for 200, so the two items are one scenario with cosmetic changes rather than two calibrated variants of the maintenance-cost concept (severity: minor)."
     ],
     "fix": "Keep ta-07-b as the \"remove duplication\" variant and rewrite ta-07-a to test a different facet of the same blueprint topic (maintenance cost of automation, CTAL-TAE ch.4 and ch.8) at the same difficulty and length, for example: \"A team automates the same regression checks in four ways described below. After each release the application's screen layout changes but its API contracts do not. Which suite will need the most maintenance effort per release?\" with four one-line suite descriptions (UI tests locating elements by on-screen position; UI tests using a shared identifier map; API tests calling endpoints directly; API tests via a shared client library), key = tests tied to on-screen position, rationale explaining that maintenance cost rises with coupling to the most volatile interface and why each other suite is cheaper to maintain. Keep the two-sentence set-up, four plain options and the same sourceAnchor so the pair stay interchangeable in topic and difficulty while no longer sharing a template."
    }
   ]
  },
  {
   "item": "ta-07-b",
   "revised": 1,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "Option b \"Keep the sequence written out in every test so that each change can be checked test by test.\" is not a \"change to the suite's design\" as the stem asks, so it is eliminable from the stem's wording alone rather than from knowledge of maintainability, making it a weak distractor.",
      "The stem's measure \"reduces the effort of the next such change\" is absolute rather than tied to a countable quantity, which leaves option d \"Re-record the login sequence in each test with a capture and replay tool after each change.\" open to the argument that re-recording is quicker per test than hand-editing and therefore also reduces effort, even though it still touches all 200 tests."
     ],
     "fix": "Change the last sentence of the stem to \\\"Which change to the suite's design reduces the number of tests that have to be edited at the next such change?\\\" so that the measure is countable (a: none, b: 200, c: more than 200, d: 200 re-recorded) and option d has no foothold. Replace option b with a genuine design change that still keeps one copy per test, for example \\\"Store each test's copy of the login sequence in a separate file kept next to that test.\\\" and update the rationale for b to say it keeps 200 copies, so the next change is still 200 edits. Apply the same two adjustments to the sibling ta-07-a to keep the slot interchangeable."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "Stem sentence \"A suite of 200 automated API tests each begins with the same eight-step login sequence, written out in full inside every test.\" has a subject-verb mismatch: the grammatical subject is \"A suite\" but \"each begins\" refers to the tests, which breaks the charter's plain-sentence rule and makes the reader re-parse the opening sentence."
     ],
     "fix": "Rewrite the first stem sentence as: \"Each of the 200 automated API tests in a suite begins with the same eight-step login sequence, written out in full inside every test.\" Leave the rest of the stem, the options, the rationale and the sourceAnchor unchanged."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The stem \"A suite of 200 automated API tests each begins with the same eight-step login sequence, written out in full inside every test. The login sequence changes and all 200 tests have to be edited. Which change to the suite's design reduces the effort of the next such change?\" is the sibling ta-07-a's scenario (\"A suite of 300 automated UI tests ... each test holds its own copy of every identifier ... all 120 tests have to be edited. Which change to the suite's design reduces the effort of the next such rename?\") with only the nouns and numbers changed, so the two items are not independent draws for the slot and a retake participant would effectively see the same item twice. [major]",
      "The key \"Move the login sequence into one shared function that every test calls.\" and distractors \"Keep the sequence written out in every test ...\" and \"Re-record the login sequence in each test with a capture and replay tool after each change.\" reproduce the sibling's option set (\"Hold each identifier in one shared place ...\", \"Keep a copy of each identifier in every test ...\", \"Re-record the affected tests with a capture and replay tool after each rename.\") one for one, so the slot tests only the single sub-concept of removing duplication rather than the blueprint's broader \"Maintenance cost of automation - concept\". [major]",
      "The rationale \"with the sequence held once and called from everywhere, a change becomes one edit instead of 200, which is the maintainability principle of removing duplication from the automation solution ... d is wrong because capture and replay reproduces the duplication in recorded form\" is a near-verbatim copy of ta-07-a's rationale, which confirms the item was produced by cosmetic substitution rather than written as a distinct calibrated item. [major]",
      "Distractor c \"Split each test into several smaller tests, each with its own copy of the sequence.\" announces its own flaw in the phrase \"each with its own copy\", so it is eliminable on wording alone and is not a plausible pick for a genuinely unsure candidate. [minor]"
     ],
     "fix": "Rewrite ta-07-b to test a different facet of maintenance cost at the same level, keeping the read-a-scenario style but changing what the candidate must recognise. For example: describe a suite whose tests embed the test environment's base URL, the current release's expected version string, and fixed one-second waits directly inside every test; state that the team is about to move the suite to a second environment; and ask which property of the suite means that move will require an edit in every test (key: the base URL is written inside each test; distractors: the fixed one-second waits, the expected version string, the number of tests in the suite). The rationale must then explain why each of the other properties either does not change with the environment move or does not force per-test edits. Keep the sourceAnchor as ISTQB CTAL-TAE v2 Chapter 4 maintainability. Alternatively, keep the current scenario but replace distractor c with a plausible wrong remedy that does not reduce the number of edits, such as \"Tag every test that uses the login sequence so that the affected tests can be found quickly after a change\", and rewrite the rationale in fresh words; however this alternative still leaves the item too close to ta-07-a and the rewrite is preferred."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Major: the question \"Which property of the suite means that this move requires an edit in every test?\" is answerable from the stem alone, because the stem labels option a as \"the base URL of the test environment it calls\" and then says the team will \"run the suite against a second test environment\", so a candidate needs no knowledge of maintenance cost to pick the key; this makes ta-07-b markedly easier than ta-07-a, which requires reasoning about which of four designs is coupled to the part of the application that changes, so the two items are not interchangeable in difficulty as docs/03 and docs/04 require.",
      "Minor: the discriminating knowledge tested is which of four values differs between two environments (configuration management, chapter 5) rather than the slot 7 blueprint topic \"Maintenance cost of automation - single - concept\"; the maintenance-cost principle (\"A copy of a value in every test costs maintenance only when that value changes\") is stated only in the rationale and is never what the candidate has to apply.",
      "Minor: distractor d, \"The account number of the test customer is written into every test.\", leaves a dispute route open because experienced testers know account identifiers are often generated on load and differ between environments; the stem's \"loaded with the same test data\" does not explicitly close this, so a candidate could defend d.",
      "Minor: the stem of ta-07-b is roughly 85 words against roughly 55 for ta-07-a, so the item takes noticeably longer to read within a 10-minute paper."
     ],
     "fix": "Rewrite ta-07-b so the candidate must apply the maintenance-cost principle rather than identify the changing value, keeping the environment-move scenario but making the answer depend on how many places a changing value lives. For example: \"A suite of 150 automated API tests is spread across 10 test files. The base URL of the test environment is written once at the top of each test file and every test in that file reads it from there. The team is about to point the suite at a second test environment that hosts the same release of the system loaded with the same test data, including the same customer account numbers. How many edits does this move require?\" Options, fixedOrder true, ascending: 1, 10, 150, 600. Key: 10. Rationale: the base URL is the only value that differs between the environments and it is held in exactly one place per file, so the move costs one edit per file; 1 would require the value to be held in a single configuration read at run time; 150 would require a copy in every test; 600 counts every hardcoded value even though only the base URL changes. This keeps the item on the blueprint concept (maintenance cost scales with how many places a changing value is duplicated), makes it read-and-predict, closes the account-number dispute with the added clause, shortens the stem to a length comparable with ta-07-a, and is not a cosmetic variant of ta-07-a because it tests duplication count rather than coupling to the changing layer."
    }
   ]
  },
  {
   "item": "ta-08-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "Option d \"The number of tests that passed in the same run.\" can be argued by a competent candidate as useful investigation context (it shows whether the failure was isolated or environment-wide), so its exclusion from the key is contestable under the stem's criterion \"so that the failure can be investigated later without re-running the test\"."
     ],
     "fix": "Replace option d with a distractor that is clearly not failure evidence and cannot be read as triage context, for example \"The average duration of the suite over the last month.\" (matching the style of ta-08-b option e), and update the rationale sentence for d accordingly; apply the same replacement to the equivalent option d in bank/drafts/ta/ta-08-b.json so the two items stay interchangeable."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Minor alignment drift: the scenario \"An automated checkout test fails during a nightly run of a web shop's suite\" with key option \"The log output and screenshot captured at the moment of failure\" frames a UI-level test, whereas docs/04's governing principle targets the programme's back-end, data-focused automation use case and the sibling ta-08-b is framed around a data pipeline reconciliation check, so the two items are not fully interchangeable in framing (a candidate from a UI-automation background is more at home with ta-08-a than with ta-08-b)."
     ],
     "fix": "Reframe ta-08-a to a back-end scenario of the same shape and difficulty, for example: stem \"An automated API test for an order-creation endpoint fails during a nightly run. Select all the pieces of information the failure record should contain so that the failure can be investigated later without re-running the test.\"; keep options a (expected and actual value at the failed step), b (build identifier and environment) and d, e, f unchanged, and change option c to \"The request sent and the response received at the moment of failure, with the log output.\" Update the rationale's sentence for c accordingly. Everything else (structure, key count, anchor, distractor set) is sound and matches the sibling."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "Structural cue: all three keyed options are compound two-part clauses of 64 to 76 characters (\"The expected value and the actual value at the step that failed.\", \"The build identifier of the system under test and the environment it ran in.\", \"The log output and screenshot captured at the moment of failure.\") while all three distractors are short single-fact clauses of 39 to 48 characters (\"The number of tests that passed in the same run.\", \"The total number of tests in the suite.\", \"The sprint in which the test was first written.\"), so a test-wise candidate can select the key set by option shape alone, which weakens the charter requirement that distractors be plausible picks for a genuinely unsure person.",
      "Scenario framing: the stem \"An automated checkout test fails during a nightly run of a web shop's suite\" and the keyed option \"The log output and screenshot captured at the moment of failure\" describe a UI web-shop test, whereas docs/04's governing principle anchors items to the programme's back-end, data-focused automation use case and the sibling ta-08-b uses a data pipeline check, so the two items in the slot are not fully interchangeable in topic."
     ],
     "fix": "Equalise option shape so structure does not reveal the key: make at least one distractor a compound clause of similar length (for example \"The name of the team that owns the test and the date it was added to the suite.\") and shorten one keyed option (for example \"The build and environment the test ran against.\"). Reframe the scenario toward the programme's back-end focus and align with ta-08-b: for example \"An automated API test for order creation fails during a nightly run\" with option c reading \"The log output and the response payload captured at the moment of failure.\" Keep the same three distractor types (run pass count, suite size, test origin history) so difficulty stays matched with ta-08-b."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Distractor d, \"The average duration of the suite over the last month.\", is substantively the same distractor as sibling ta-08-b's option e, \"The average duration of the pipeline over the last month.\", and combined with the identical framing in both stems (a nightly run fails, the failure is investigated later without re-running, what must the record hold) the two items differ only cosmetically in scenario and option pattern, which weakens their independence as interchangeable draws for the slot (minor)."
     ],
     "fix": "In ta-08-a replace option d with a distractor not used by ta-08-b, for example \"The number of times the test has passed since it was added to the suite.\", and vary the framing so the items are not the same template, for example open the stem with \"An automated API test for an order-creation endpoint fails in the pipeline run triggered by a code change.\" while keeping the \"investigated later without re-running the test\" criterion; update the rationale for the new option d accordingly."
    }
   ]
  },
  {
   "item": "ta-08-b",
   "revised": 1,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The item is a template clone of sibling ta-08-a rather than an independently written alternative: the stem \"fails during a scheduled run of a data pipeline. Select all the pieces of information the failure record should contain so that the failure can be investigated later without re-running the check.\" reproduces ta-08-a's stem word for word apart from the scenario nouns, and distractors d \"The number of checks that passed in the same run.\" and f \"The total number of checks in the suite.\" are ta-08-a's d and e with \"tests\" replaced by \"checks\", with the rationale sentences for d and f also copied, so the two items in the slot differ only cosmetically in their wrong-answer set and a participant who has discussed one item with a colleague has effectively seen the other."
     ],
     "fix": "Keep the reconciliation-check scenario and the three key options, but rewrite the stem in its own words (for example: \\\"A scheduled data pipeline run reports one failed reconciliation check. Select all the items the failure record must hold so that an investigator can work from the record alone.\\\") and replace distractors d and f with plausible items that do not appear in ta-08-a, such as \\\"The name of the team member who wrote the check.\\\" and \\\"The planned date of the next change to the pipeline.\\\", then update the rationale so that each wrong option is explained in wording that is not copied from the sibling. Sibling ta-08-a can keep its current distractors."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Option e \"The average duration of the pipeline over the last month.\" is a near-verbatim reuse of sibling ta-08-a's distractor \"The average duration of the suite over the last month.\", so the two items share a distractor with only a cosmetic word change rather than being independently written.",
      "Option f \"The planned date of the next change to the pipeline.\" is weaker than every distractor in sibling ta-08-a because it refers to something that has not yet happened, so it reads as filler that an unsure candidate would not pick, making ta-08-b marginally easier than ta-08-a.",
      "The stem runs to four sentences (\"A nightly data pipeline includes ... On one night ... The next morning ... Select all the items ...\") against ta-08-a's two-sentence stem, so the two siblings are not matched in length."
     ],
     "fix": "Replace option e with a distractor not shared with ta-08-a, for example \"The number of checks in the pipeline and how many of them passed that night.\", and replace option f with a plausible but still run-independent distractor, for example \"The schedule that decides which nights the pipeline runs.\", keeping the correct set as a, b and c. Tighten the stem to two sentences to match ta-08-a, for example: \"A nightly reconciliation check that compares the rows a pipeline loaded with the rows in the source reports a failure, and the investigator has only the failure record and cannot re-run the check. Select all the items the record needs to hold for the investigator to establish what went wrong.\" Update the rationale to explain the new distractors."
    },
    {
     "lens": "Key defensibility",
     "problems": [
      "Key option b, \"The identifier of the pipeline run and the time at which the check failed.\", is contestable under the stem wording \"has only the failure record to work from\" and \"the items the record needs to hold ... to establish what went wrong\", because a run identifier and timestamp are pointers to other systems' logs the investigator is stated not to have, and the failing night is already given, so a competent candidate can defensibly exclude b as context rather than necessary evidence and lose the all-or-nothing point.",
      "The rationale rejects d with \"a fact about the check's ownership that is the same on every run\", yet option a's \"The rule that was evaluated\" is also the same on every run (the stem itself fixes the rule as comparing loaded rows with source rows), so the rationale's own test undermines part of the key and would not hold up as a dispute record.",
      "Distractor f, \"The planned date of the next change to the pipeline.\", is near-absurd filler that no genuinely unsure candidate would pick, which weakens the item against the charter's plausibility requirement."
     ],
     "fix": "Soften the stem to the sibling's pattern, e.g. \"Select all the pieces of information the failure record should contain so that the failure can be investigated later from the record alone.\", and reword option b so its evidentiary value is self-contained, e.g. \"The version of the pipeline code and the environment the check ran against.\" Reword option a to \"The identifiers of the source and loaded datasets the check compared.\" so it names run-specific evidence only, and adjust the rationale for d to say the author is not evidence about the data or outcome of this run rather than relying on the \"same on every run\" argument. Replace distractor f with a plausible but wrong item such as \"The number of rows the pipeline loaded on each of the previous seven nights.\" or \"The list of downstream reports that consume the loaded table.\""
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "The stem's wording \"Select all the items the record needs to hold for the investigator to establish what went wrong\" sets a strict necessity test that a candidate could use to dispute option b (\"The identifier of the pipeline run and the time at which the check failed\"), arguing that run identity and time aid traceability but are not strictly needed to establish what went wrong once the rule, its input identifiers and the mismatched rows are recorded."
     ],
     "fix": "Reword the final sentence of the stem to match the blueprint's \"useful failure record\" framing and the sibling ta-08-a, for example: \"Select all the pieces of information the failure record should contain so that the failure can be investigated later from the record alone.\" Leave options, key, rationale and sourceAnchor unchanged."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Minor: option e \"The average duration of the pipeline over the last month.\" is a near-verbatim copy of sibling ta-08-a's option d \"The average duration of the suite over the last month.\", so the two items share a distractor rather than being independently calibrated.",
      "Minor: option f \"The planned date of the next change to the pipeline.\" is closer to absurd filler than a plausible wrong answer, because a future event obviously cannot explain a past failure (the rationale itself says so), which makes ta-08-b marginally easier than ta-08-a whose distractors are all present-tense suite metadata an unsure person might include.",
      "Minor: the stem frames the failing artefact as \"a reconciliation check\" in \"a nightly data pipeline\" rather than an automated test, and at roughly 60 words it is about half as long again as the sibling's 40-word stem, so the item drifts slightly from the slot's automated-test framing and is longer than its sibling."
     ],
     "fix": "Keep the scenario but tighten the stem to about 40 words and name the check as an automated test, for example: \\\"An automated reconciliation test in a nightly data pipeline compares the rows loaded with the rows in the source. One night it fails. The next morning an investigator has only the failure record and cannot re-run the test. Select all the items the record needs so the investigator can establish what went wrong.\\\" Replace option e with a distractor not shared with ta-08-a, for example \\\"The number of rows the pipeline loaded successfully on previous nights.\\\", and replace option f with a present-tense plausible distractor such as \\\"The list of downstream reports that read from the loaded table.\\\" Update the rationale for the new e and f accordingly."
    },
    {
     "lens": "Key defensibility",
     "problems": [
      "Option b \"The identifier of the pipeline run and the time at which the check failed.\" is contestable as a required item because the stem restricts the goal to \"establish what went wrong\" while stating the investigator \"has only the failure record to work from\", so a candidate can defensibly argue that a and c alone establish what went wrong and that a run identifier is only useful for cross-referencing records the investigator does not have (major).",
      "Option a \"The rule that was evaluated and the identifiers of the input data it used.\" bundles a run-invariant fact (the rule) with a run-specific one, and the rationale for d rejects the author precisely because it \"is the same on every run\", so a candidate can apply the item's own reasoning to argue the rule need not be in the record (major).",
      "Option e \"The average duration of the pipeline over the last month.\" is a near-verbatim copy of sibling ta-08-a option d \"The average duration of the suite over the last month.\", so a participant who is reset and served the sibling sees a recycled distractor (minor)."
     ],
     "fix": "Reword the stem's final sentence to make the run-locating purpose explicit, for example: \"Select all the items the record needs to hold for the investigator to establish which run failed and what went wrong.\" Keep \"cannot re-run the check\" and \"has only the failure record to work from\" so looking elsewhere is excluded. Reword option a to contain only run-specific content, for example: \"The identifiers of the source data set and the loaded data set that the check compared.\" Update the rationale for a and b to match (a: which data was compared in this run; b: which execution and when, which the stem now asks for). Replace option e with a distractor not used by ta-08-a, for example: \"The number of rows the pipeline loaded on each of the previous seven nights.\""
    }
   ]
  },
  {
   "item": "ta-09-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Duplication with sibling (minor): four of the five scenarios in ta-09-a (\"A fast unit suite guarding every merge.\", \"A nightly full regression across environments.\", \"A full smoke run immediately before a production deployment.\", \"A rerun to verify one specific defect fix.\") reappear in ta-09-b as cosmetic rewrites (\"A quick unit suite that starts automatically every time a developer pushes a change to the shared branch.\", \"A full regression of the payments batch that runs every night at 02:00.\", \"A final end-to-end run of the release candidate immediately before it is promoted to production.\", \"A run started by a tester to confirm that one reported defect has been fixed.\"), so the slot is one scenario set in two wordings rather than two interchangeable items.",
      "Calibration drift (minor): every ta-09-b token states the initiating event explicitly (\"starts automatically every time a developer pushes\", \"runs every night at 02:00\", \"started by a tester\") whereas ta-09-a's tokens are shorter and leave the initiator to inference, most notably \"A rerun to verify one specific defect fix.\" which names no person or event starting it, so ta-09-a is marginally harder and shorter than its sibling.",
      "Topic edge (minor): token t5 \"A static analysis check that starts automatically on every push to the shared branch.\" describes a pipeline quality gate rather than an automated test run, a small drift from the blueprint's \"Run triggers\" which concern when automated tests execute."
     ],
     "fix": "Keep the exemplar wording as the calibration reference but give ta-09-a its own scenario set so it does not mirror ta-09-b, and make each token name its initiator in the same explicit way ta-09-b does. For example: t1 (On commit) \\\"A component test suite that starts automatically each time a change is merged to the shared branch.\\\"; t2 (Scheduled) \\\"A full regression that runs every weeknight at 01:00 across the test environments.\\\"; t3 (Pre-release) \\\"A go/no-go regression pack run once the release candidate is tagged and before it is deployed to production.\\\"; t4 (On demand) \\\"A rerun started by a tester to verify one specific defect fix.\\\"; t5 (On commit) \\\"An API test suite that starts automatically on every push to the shared branch.\\\" Update the rationale to match the new tokens and keep the sourceAnchor unchanged."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Four of the five tokens duplicate sibling ta-09-b with only cosmetic changes: \"An API test suite that the pipeline starts automatically on every push to the shared branch\" mirrors b's \"An API test suite triggered by each commit to the main branch\", \"A database reconciliation suite that runs at 06:00 every morning\" mirrors b's \"A data quality check that runs every weekday at 06:00\", \"A full regression pack ... as the last gate before it is deployed to production\" mirrors b's \"A full regression run as the final check before release approval\", and \"A run that a tester starts by hand to reproduce a failure\" mirrors b's \"A run a tester starts manually to reproduce a reported defect\", so a reset participant would effectively see the same item again (minor).",
      "The item is roughly twice the reading load of its sibling (about 92 words of token text against about 51 in ta-09-b), with \"A full regression pack that the release pipeline runs against the release candidate as the last gate before it is deployed to production.\" alone at 26 words, which is a length mismatch within a one-minute-per-item drag and drop slot (minor).",
      "The two On commit tokens are near-duplicates of each other, \"starts automatically each time a change is merged to the shared branch\" and \"starts automatically on every push to the shared branch\", so the doubled bucket adds reading without adding discrimination, whereas the sibling doubles Scheduled with two genuinely different schedules (minor)."
     ],
     "fix": "Keep the buckets and the exemplar stem, but re-dress the scenarios so they differ from ta-09-b in suite type, timing and purpose, and trim every token to 10 to 14 words. For example: t1 On commit \"A contract test suite that runs automatically whenever a change lands on the shared branch.\"; t2 Scheduled \"A batch reconciliation suite that runs on the first day of every month.\"; t3 Pre-release \"A data migration rehearsal run against the release candidate the day before go-live.\"; t4 On demand \"A single test a tester starts by hand to confirm one defect fix.\"; t5 Scheduled \"An end-to-end suite that runs every night at 02:00 against the test environment.\" (this makes Scheduled the doubled bucket with two distinct schedules, or keep On commit doubled but give the second token a different cue such as \"on every pull request merge\"). Update the rationale to match the new tokens."
    }
   ]
  },
  {
   "item": "ta-09-b",
   "revised": 1,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Tokens t1 to t4 (\"A quick unit suite that starts automatically every time a developer pushes a change to the shared branch.\", \"A full regression of the payments batch that runs every night at 02:00.\", \"A final end-to-end run of the release candidate immediately before it is promoted to production.\", \"A run started by a tester to confirm that one reported defect has been fixed.\") are one-to-one paraphrases of sibling ta-09-a's t1 to t4 (unit suite guarding every merge, nightly full regression, smoke run immediately before production deployment, rerun to verify one defect fix), so the item duplicates its sibling with cosmetic changes and adds no independent bank depth.",
      "Token length is not calibrated with the sibling: ta-09-b's t1 \"A quick unit suite that starts automatically every time a developer pushes a change to the shared branch.\" is 18 words against ta-09-a's equivalent \"A fast unit suite guarding every merge.\" at 7 words, and the same roughly two-to-one ratio holds across all five tokens, so the reading load differs between interchangeable items.",
      "Token t2 \"A full regression of the payments batch that runs every night at 02:00.\" names a specific system (\"the payments batch\") in a financial domain, which is unnecessary for the trigger being tested and drifts from the generic framing the sibling uses (\"A nightly full regression across environments.\")."
     ],
     "fix": "Keep the stem, buckets and the two-Scheduled-tokens structure, but replace t1 to t4 with scenarios that differ in substance from ta-09-a (different suite types, actors and situations rather than rewordings), and trim every token to roughly 7 to 12 words to match the sibling. For example: t1 (On commit) \"An API contract suite the pipeline runs whenever a pull request is opened.\"; t2 (Scheduled) \"A data quality check that runs every Monday at 06:00.\"; t3 (Pre-release) \"A sign-off regression run as the final gate before a release is approved.\"; t4 (On demand) \"A run a tester launches to reproduce a defect reported by support.\"; t5 (Scheduled) \"A performance suite that runs every Sunday at 03:00 against the test environment.\" Remove \"the payments batch\" so no token points at a named system or sector. Update the rationale to describe the new tokens in the same per-token form."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Tokens t2, t3 and t4 (\"A data quality check that runs every weekday at 06:00.\", \"A full regression run as the final check before release approval.\", \"A run a tester starts manually to reproduce a reported defect.\") reproduce sibling ta-09-a's scenarios with cosmetic rewording (a: \"runs at 06:00 every morning\", \"the last gate before it is deployed to production\", \"a tester starts by hand to reproduce a failure\"), so the two items differ only in which bucket is doubled and a retake paper could serve what is effectively the same item.",
      "Token t1 \"An API test suite triggered by each commit to the main branch.\" contains the bucket label word \"commit\", giving a lexical cue that sibling ta-09-a withholds (\"merged to the shared branch\", \"on every push\"), which makes this item marginally easier than its sibling; the tokens are also about half the length of the sibling's, so the items are not fully interchangeable in length."
     ],
     "fix": "Keep the buckets, stem and the Scheduled doubling, but replace the near-duplicate scenarios with distinct ones of similar length to ta-09-a, avoiding bucket label words in the token text. For example: t1 \\\"A contract test suite that the pipeline starts automatically whenever a change is pushed to the main branch.\\\" (On commit); t2 \\\"A data quality check that runs against the test environment at 06:00 every weekday, whether or not anything has changed.\\\" (Scheduled); t3 \\\"A user acceptance regression pack run against the release candidate in the staging environment as the last check before go-live is approved.\\\" (Pre-release); t4 \\\"A load test that a team lead asks to be run once after a user reports the test environment is slow.\\\" (On demand); t5 \\\"A performance suite that runs every Sunday at 03:00 against the shared test environment.\\\" (Scheduled). Update the rationale to match the new token texts."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Major: ta-09-b duplicates its sibling ta-09-a with cosmetic changes only: identical stem and buckets, and four of five tokens are the same scenarios reworded, namely \"An API test suite triggered by each commit to the main branch.\" (ta-09-a t5: \"An API test suite that the pipeline starts automatically on every push to the shared branch.\"), \"A data quality check that runs every weekday at 06:00.\" (ta-09-a t2: \"A database reconciliation suite that runs at 06:00 every morning\"), \"A full regression run as the final check before release approval.\" (ta-09-a t3: \"A full regression pack that the release pipeline runs ... as the last gate before it is deployed to production.\") and \"A run a tester starts manually to reproduce a reported defect.\" (ta-09-a t4: \"A run that a tester starts by hand to reproduce a failure reported on the test environment.\"); the only real difference is which bucket receives the doubled token, so the two items are not independent draws and a participant who has seen one has effectively seen the other.",
      "Minor: ta-09-b's tokens are markedly shorter and barer than ta-09-a's (for example \"A full regression run as the final check before release approval.\" at 11 words against the sibling's 25-word equivalent that names the pipeline, the release candidate and the production deployment), so the two items differ in reading load and in how much context supports the classification.",
      "Minor: ta-09-b tokens echo the bucket label wording more directly than the sibling does, notably \"triggered by each commit\" against the bucket \"On commit\" and \"before release approval\" against \"Pre-release\", which makes ta-09-b slightly easier to match by word-spotting than ta-09-a, whose tokens use \"merged\", \"push\" and \"deployed to production\" instead."
     ],
     "fix": "Keep the stem, buckets and structure but rewrite ta-09-b with a fresh set of five scenarios that do not reuse the API suite / 06:00 data check / full regression before release / tester reproducing a defect quartet, and that avoid the literal words \"commit\" and \"release\" in the tokens. For example: t1 (On commit) \"A static analysis and unit test job that the pipeline starts whenever a developer pushes a change to a feature branch.\"; t2 (Scheduled) \"An accessibility scan of the test environment that runs every Monday at 02:00 whether or not anything has changed.\"; t3 (Pre-release) \"A smoke pack that the pipeline runs against the release candidate immediately before the go-live sign-off.\"; t4 (On demand) \"A single end-to-end journey that a tester runs by hand after an environment restart to confirm the environment is usable.\"; t5 (Scheduled) \"A licence expiry check that runs on the first day of every month at 07:00.\" Bring the token length up to roughly match ta-09-a (15 to 25 words each with the defining property named explicitly), then update the rationale to cover each new token and why the other three buckets do not fit."
    }
   ]
  },
  {
   "item": "ta-10-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "Option b \"Remove the test from the suite, because a test that fails intermittently has no value.\" pairs an action that overlaps the recognised practice of quarantining a flaky test from the regular run (named in the anchored chapter 7 topic) with a wrong justification, so a candidate who knows the quarantine practice can argue the action half of the option is legitimate and that only the attached reasoning was being tested."
     ],
     "fix": "Reword option b so the action itself is clearly wrong rather than only the justification, for example: \"Delete the test permanently and do not replace the coverage it provided, because an intermittent failure cannot be diagnosed.\" Keep the key (a), options c and d, and the rationale unchanged apart from updating the sentence about b."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The scenario \"An automated checkout test in a web shop's suite\" frames the item around a UI e-commerce test, whereas the governing principle in docs/04 targets a back-end, data-focused automation use case and the sibling ta-10-b is set in \"a data pipeline's nightly run\", so the two items differ in domain framing even though they match in topic and difficulty."
     ],
     "fix": "Reframe the stem in data or API terms while keeping every option and the rationale unchanged, for example: \"An automated check that compares a nightly extract against its source passes on most runs and fails on about one run in ten. Nothing in the check, the input data or the system under test changes between runs. Which response fits this situation?\""
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "Option d \"Raise a defect against the checkout feature at each failure without investigating the test.\" carries its own rebuttal: the clause \"without investigating the test\" is the direct negation of the key's \"investigate the cause\", so a candidate can identify the key by contrast rather than by knowing the concept, which makes the distractor self-disqualifying filler rather than a plausible wrong answer.",
      "Option b \"Remove the test from the suite, because a test that fails intermittently has no value.\" appends an absurd justification (\"has no value\") that no genuinely unsure professional would endorse, so the distractor is weakened to filler; the removal action alone is the plausible mistake, the stated reason is not.",
      "The scenario \"An automated checkout test in a web shop's suite\" is a UI-flavoured e-commerce setting, while docs/04's governing principle targets the programme's back-end, data-focused use case and the sibling ta-10-b uses a data pipeline check; the two items are interchangeable in difficulty but the framing of ta-10-a is less aligned with the stated use case."
     ],
     "fix": "Rewrite the two weak distractors so they state a plausible action without a self-refuting reason, for example option d: \"Raise a defect against the checkout feature each time the test fails, since a failing test shows the feature is broken.\" and option b: \"Delete the test and rely on the remaining checkout tests for that coverage.\" Optionally reframe the scenario to a back-end check to match the sibling, for example: \"An automated check that compares an order-total report against its source table passes on most nightly runs and fails on about one run in ten.\" Keep the key and rationale as they are, updating the rationale wording for b and d to match the new option text."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Minor: the key \"Treat the check as unreliable and investigate the cause of the varying result before trusting it.\" places the presumed fault in the check, while distractor d is marked wrong in the rationale precisely because it \"assumes the extract job is at fault without evidence\"; the sibling ta-10-b avoids this asymmetry by keying on the results being unreliable (\"the cause of its inconsistent results must be found before any of them is relied on\"), and a candidate who reasons that the check may be correctly detecting an intermittent fault in the job could argue the key is as presumptuous as d.",
      "Minor: the item is structurally very close to its sibling ta-10-b, with the same scenario frame (a nightly automated data check that intermittently fails while \"Nothing in the check, the test data or the extract job changes between runs\") and two of its three distractors sharing the same wrong idea as the sibling's (\"retry the check until it passes and report the eventual pass\" mirrors b's \"run it again after each failure and report the eventual pass\", and \"Raise a defect against the extract job at each failure\" mirrors b's \"detected a new pipeline defect on Monday and a second new pipeline defect on Thursday\"), so the two items differ mainly in the failure pattern described rather than in scenario.",
      "Minor: the blueprint for slot 10 is \"recognise the concept and the correct response\", and the sibling's key names the concept (\"It is a flaky check\") while this item's key does not, so the two items are not quite equal in what they ask the candidate to recognise; a candidate who knows the term has a slightly easier task on ta-10-b than on ta-10-a."
     ],
     "fix": "Reword the key so it targets the results rather than the check as the thing not to be trusted and names the concept, in line with the sibling: \"Recognise the check as flaky and find the cause of its varying result before relying on any of its results.\" Then differentiate the scenario from ta-10-b more clearly, for example by moving away from a nightly data job to a check run on every commit in a pipeline (an API-level comparison against a reference dataset that fails intermittently across commits that touched unrelated code), and replace one of the two shared distractors with a different plausible wrong response, such as \"Mark the check as expected to fail so that the run stays green until someone has time to look at it\", keeping the retry-until-pass distractor as the one deliberate overlap."
    }
   ]
  },
  {
   "item": "ta-10-b",
   "revised": 1,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "The rationale's refutation of b, \"a genuine defect in an unchanged pipeline with unchanged data would fail every night, not on two nights out of five\", overclaims because intermittent defects in an unchanged system (race conditions, resource contention) do exist, so a disputing candidate could attack the written record even though option b remains wrong on the stronger ground that a \"new defect\" cannot appear in a pipeline that does not change.",
      "The key text \"its result varies with no change to the check or the pipeline\" restates the stem sentence \"Nothing in the check, the input data or the pipeline changes during the week\" almost verbatim, giving a surface cue that lets a candidate pick the key by wording match rather than by recognising the concept, and it appends a directive (\"so the cause must be found before it is trusted\") although the stem asks only \"Which description fits this check?\"."
     ],
     "fix": "Rewrite the rationale for b as: \"b is wrong because the pipeline does not change during the week, so no new defect can have been introduced, and the same fault cannot be found as new twice; the stem gives no evidence that the cause lies in the pipeline rather than in the check or its environment, which is exactly what investigation must establish.\" Reword option a so it names the concept without echoing the stem, for example: \"It is flaky: the same check on the same pipeline and data gives different results from run to run, so its outcome cannot be trusted until the cause is found.\" Keep the option length comparable to the distractors."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The key option \"It is flaky: its result varies with no change to the check or the pipeline, so the cause must be found before it is trusted.\" restates the stem's own premise (\"Nothing in the check, the input data or the pipeline changes during the week\") almost word for word and is the only option that names the concept, so a candidate can pick it by matching wording to the stem rather than by recognising flakiness, which makes ta-10-b markedly easier than sibling ta-10-a, whose key (\"Treat the test as unreliable and investigate the cause of the varying result before trusting it.\") carries no such echo and sits among three responses teams genuinely take.",
      "Distractors c \"It is a passing check, because it passed on more days than it failed, so the failures can be ignored.\" and d \"It is a failing check, because any failure during the week means the pipeline is broken.\" are not answers an experienced professional tester would plausibly choose, leaving b as the only real competitor to the key, whereas ta-10-a's distractors (remove the test, retry until it passes, raise a defect each time) are all common real-world responses; the two items are therefore not interchangeable in difficulty as docs/03 and docs/04 require."
     ],
     "fix": "Bring ta-10-b to the same bar as ta-10-a by removing the stem echo from the key and replacing the two weak distractors with plausible real-world responses. Suggested key: \"It is flaky, and the cause of the varying result must be found before it is trusted.\" Suggested replacements: c \"It is timing-dependent, so the run should retry it once and report the eventual pass.\" and d \"It is unreliable, so it should be removed from the nightly run to keep the run green.\" Keep b as is (a genuine defect would fail every night with unchanged data and pipeline). Update the rationale to explain why each new distractor is wrong: retrying hides the non-determinism and would also hide a real intermittent fault; removing the check discards its coverage without learning whether the cause is in the check or the pipeline. This keeps the item on the blueprint topic (recognise the concept and the correct response), keeps it distinct from ta-10-a in scenario and framing, and equalises difficulty across the slot."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "Major: the rationale for b states that \"a genuine defect in an unchanged pipeline with unchanged data would fail every night, not on two nights out of five\", which is factually contestable because intermittent defects in the system under test (race conditions, resource exhaustion, timing) are a recognised cause of non-deterministic results, so a candidate who chose b could attack the dispute record on that ground; the sibling ta-10-a correctly says \"the cause may be in the test itself, and only investigation can tell\".",
      "Minor: key a echoes the stem's own premise (\"its result varies with no change to the check or the pipeline\" restates \"Nothing in the check, the input data or the pipeline changes\") and is the only option carrying a two-part justification with a colon, giving a stem-echo and longest-option cue that makes this item easier than sibling ta-10-a and weakens slot interchangeability.",
      "Minor: distractor c ends \"so the failures can be ignored\", which no genuinely unsure professional would pick, so it reads as absurd filler rather than a plausible wrong answer.",
      "Minor: distractor b combines a true classification (\"It is a regression check\", which a nightly check on unchanged functionality is) with a false claim (\"found a new defect ... on Monday and again on Thursday\"), so a candidate can argue the first clause is a correct description of the check; the wrong part should carry the whole option."
     ],
     "fix": "Rewrite the b rationale to rebut on the option's own wording rather than on a false general claim, for example: \\\"b is wrong because nothing in the pipeline changed, so no new defect could have been introduced on Monday and again on Thursday; the varying result shows the check cannot yet be trusted, and only investigation can tell whether the cause lies in the check or in the pipeline.\\\" Shorten key a so it does not restate the stem, for example: \\\"It is a flaky check, and the cause of the varying result must be found before its results are trusted.\\\" Replace c with a plausible wrong practice, for example: \\\"It is a passing check, because Friday's run is the latest result and the latest result is the one that counts.\\\" Replace b's true classification with a wholly wrong claim, for example: \\\"It is a check that has detected a new pipeline defect on Monday and another on Thursday.\\\""
    },
    {
     "lens": "Key defensibility",
     "problems": [
      "Minor: the key labels the situation \"It is a flaky check\" while the rationale concedes \"the cause may be in the check, in its environment or in the pipeline\"; a candidate who understands flakiness as a property of the test rather than of the system could hesitate over attributing the flakiness to the check, although none of b, c or d is defensible (b requires a changed pipeline, which the stem rules out; c ignores two failures; d's \"only needs a retry rule\" hides rather than resolves the inconsistency), so the key remains the sole defensible option."
     ],
     "fix": "Reword option a so it describes the results rather than assigning the flakiness to the check, for example: \"Its results are flaky (non-deterministic), and the cause of the inconsistency must be found before any of the week's results is relied on.\" Keep b, c and d as they are."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "ta-10-b reuses the sibling ta-10-a scenario skeleton with cosmetic changes: both describe a nightly data check that intermittently fails while \"The check, the input data and the pipeline are the same\" (ta-10-a: \"Nothing in the check, the test data or the extract job changes between runs\"), both key on \"flaky, find the cause before trusting\", and both carry the same distractor \"run it again after each failure and report the eventual pass\" (ta-10-a: \"retry the check until it passes and report the eventual pass\").",
      "Option c, \"It is a passing check, because Friday's run is the most recent result and the most recent result is the one that counts.\", is weaker than the other distractors because its trailing clause reads as filler rather than a wrong answer a genuinely unsure professional might pick."
     ],
     "fix": "Keep the item on slot 10 but move it off the nightly batch-data skeleton so the two siblings cover distinct flakiness patterns: for example, an automated check that passes when run on its own but fails on roughly one run in five when run inside the full suite, with nothing in the check or the system under test changed, and ask which conclusion fits. Replace the shared retry distractor with a differently shaped wrong conclusion (for example, that the check must be moved to run first in the suite so that it always passes), and rewrite option c as a plausible misreading such as \\\"The check has recovered on its own, so the earlier failures can be disregarded.\\\" Update the rationale to explain each new alternative."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "Option b \"It has detected a new pipeline defect on Monday and a second new pipeline defect on Thursday.\" is close to filler because the stem has just stated that the pipeline is the same every night, so the word \"new\" (twice) lets a candidate eliminate it without understanding flakiness; it is less plausible than the equivalent distractor in sibling ta-10-a (\"Raise a defect against the extract job at each failure\").",
      "Option c \"It is a passing check, because Friday's run is the most recent result and the most recent result is the one that counts.\" carries a self-refuting tail (\"and the most recent result is the one that counts\") that reads as a strawman rather than a wrong answer a genuinely unsure person would pick."
     ],
     "fix": "Rephrase option b to a plausible wrong reading that stays unambiguously wrong, for example: \"Each failure shows a fault in the pipeline on that night, so a defect should be raised against the pipeline for Monday and for Thursday.\" (rationale already explains why blaming the pipeline without investigation is wrong). Trim option c to: \"It is a passing check, because Friday's run is the most recent result.\" and keep the rationale's point that the latest result does not account for the two failures."
    }
   ]
  },
  {
   "item": "python-01-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Calibration: the list `values = [0, 1, \"\", \"no\", [], [0], None]` requires five separate falsy/truthy judgements (0, \"\", [], [0], None) each of which maps to a listed distractor, whereas sibling python-01-b requires only three judgements (\"3\" == 3, int(\"3\") == 3, \"3\" is truthy) that are each a direct Programming for Everybody lesson, so a candidate with one isolated misconception is more likely to fail python-01-a than python-01-b.",
      "Calibration: the element `[0]` (a non-empty list whose only member is falsy) tests a rule about list truthiness depending on length rather than contents that goes a step beyond the data types and conditional code material a Programming for Everybody graduate would hold, and no comparable edge case appears in python-01-b."
     ],
     "fix": "Drop the `[0]` element so the item exercises only the basic falsy kinds already at course level: use `values = [0, 1, \\\"\\\", \\\"no\\\", [], None]` (key 2), with fixedOrder options 1, 2, 3, 4 ascending. Update the rationale: 1 treats the non-empty string \\\"no\\\" as falsy (only the empty string is falsy); 3 treats one of the empty string, the empty list or None as truthy (all are falsy); 4 treats only 0 as falsy. This brings the number of independent judgements (0, \\\"\\\", [], None, plus \\\"no\\\" truthy) closer to python-01-b's three core-lesson checks while keeping the same topic, length and predict-the-output style."
    }
   ]
  },
  {
   "item": "python-01-b",
   "revised": 1,
   "fails": [
    {
     "lens": "Charter compliance",
     "problems": [
      "The rationale sentence \"10 assumes the string \\\"3\\\" is falsy; only the empty string is falsy.\" is literally false as written, because 0, None, [] and other values are also falsy in Python, so the dispute-handling record contains an inaccurate statement even though the intended meaning (among strings, only the empty string is falsy) is clear from context."
     ],
     "fix": "In bank/drafts/python/python-01-b.json, change the rationale clause to: \"10 assumes the string \\\"3\\\" is falsy; among strings only the empty string is falsy, and \\\"3\\\" is non-empty.\" No other change is needed: the key 110 is verified by execution, the structure, distractors, stem wording, fixedOrder ascending numeric options, single visual element and sourceAnchor all comply."
    }
   ]
  },
  {
   "item": "python-03-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The stem's signature \"def scale(value, factor=2):\" makes the key depend on default parameter values, a feature the anchor course's functions unit (\"functions (defining, arguments, return values)\" in docs/plan/course-syllabi.md, PY4E chapter 4: parameters and arguments, fruitful and void functions) does not teach, whereas sibling python-03-b (\"no return statement, so the call evaluates to None\") is anchored squarely to that unit, so a course graduate is less equally placed across the two items than the slot requires.",
      "The distractor \"9\", explained in the rationale as \"keeps only the second call's value and drops the first\", does not correspond to a reading mistake a genuinely unsure reader would make, so the item effectively offers three live options and is marginally easier than its sibling, whose four options each map to a real misconception."
     ],
     "fix": "Rewrite the item around positional argument binding and return values, which the course teaches directly, and keep four numeric options that each map to a real misreading. For example: stem \\\"What does this code print?\\\" with code `def diff(first, second):\\\\n    return first - second\\\\n\\\\nresult = diff(10, 4) + diff(4, 10)\\\\nprint(result)`; options ascending with fixedOrder true: \\\"-12\\\" (both calls read as second minus first), \\\"0\\\" (key: 6 + (-6)), \\\"6\\\" (only the first call counted), \\\"12\\\" (both calls read as first minus second regardless of order, 6 + 6); rationale stating that arguments bind to parameters by position so diff(10, 4) is 6 and diff(4, 10) is -6, and why each alternative is wrong; sourceAnchor unchanged. This keeps the item a read-and-predict trace of the same length and difficulty as python-03-b while removing the untaught default-argument dependency and the weak distractor."
    }
   ]
  },
  {
   "item": "python-05-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "In python-05-a the distractor \"2024 3 False\" is not a plausible error because the printed check text.endswith(\".csv\") has no trap (the rationale can only say it \"assumes endswith returns False here\"), so the item effectively has three working distractors against four in sibling python-05-b and is marginally easier."
     ],
     "fix": "Make the boolean facet carry a genuine decision so that the True/False distractor reflects a real misconception, for example replace text.endswith(\".csv\") with text.startswith(\"Invoice\") (key becomes \"2024 3 False\", distractor \"2024 3 True\" reflects forgetting that string comparison is case sensitive) and update the rationale and options accordingly."
    }
   ]
  },
  {
   "item": "python-05-b",
   "revised": 1,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The call `.lower()` in `result = code.replace(\"-\", \"\").lower()` has no effect on anything the item measures (the slice `result[2:5]` is the digits \"123\" and the length is unchanged by case), and no distractor exploits case, so it is inert reading load that the sibling python-05-a does not carry; every method in the sibling's snippet contributes to its printed output."
     ],
     "fix": "Either drop `.lower()` from the snippet (stem becomes `result = code.replace(\\\"-\\\", \\\"\\\")` with the key still \\\"123 8\\\" and the rationale trimmed accordingly), or make the case change observable by printing a slice that contains letters, for example `print(result[5:8], len(result))` with key \\\"xyz 8\\\" and distractors \\\"XYZ 8\\\", \\\"xyz 10\\\" and \\\"-xy 10\\\", updating the rationale to explain each."
    }
   ]
  },
  {
   "item": "python-06-b",
   "revised": 1,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Minor calibration drift: the snippet in python-06-b is 10 lines and chains four operations (\"rows[1:]\", \"row.split(\",\")\", \"parts[1].isdigit()\", \"int(parts[1])\") while tracking two accumulators (\"total\" and \"skipped\"), whereas python-06-a (7 lines, one accumulator, one dict lookup) and python-06-c (6 lines, one accumulator, tuple unpacking) each give a candidate fewer places to slip, so this item is slightly longer and denser than its slot siblings rather than fully interchangeable."
     ],
     "fix": "Trim python-06-b to one accumulator so its load matches its siblings: keep the header line and the rows[1:] slice (they carry the item's realistic reading value), but remove the skipped counter and the else branch, ending with print(total). Replace the options with: \"35\" (key), \"25\" (assumes the loop stops at the non-numeric row), \"10\" (assumes rows[1:] drops the first two lines) and \"The code raises a ValueError\" (assumes int() is attempted on \\\"x\\\"; the isdigit test prevents that). Update the rationale to cover the four options and keep fixedOrder false since one option is non-numeric."
    }
   ]
  },
  {
   "item": "python-06-c",
   "revised": 1,
   "fails": [
    {
     "lens": "Charter compliance",
     "problems": [
      "The sourceAnchor \"Programming for Everybody - lists and tuples with loops and conditional code (reading a short program end to end)\" names \"tuples\", a topic that does not appear anywhere in docs/plan/course-syllabi.md for PY-1 (which lists variables and expressions, conditional code, functions, loops and iteration, and strings, lists and dictionaries), so the anchor names coverage the syllabus record does not list."
     ],
     "fix": "Reword the sourceAnchor in syllabus terms, for example: \"Programming for Everybody - loops and iteration (loop idioms) with conditional code and lists (reading a short program end to end)\". The code itself can stay as written; unpacking a pair inside a for loop is a loop idiom covered under the listed loops and iteration topic, and the anchor should cite that listed topic rather than an unlisted one."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The snippet \"for name, passed in results:\\n    if not passed:\\n        failed.append(name)\" is a single boolean filter over three tuples with no data transformation, whereas sibling python-06-a requires dict key iteration plus .get on an absent key and python-06-b requires header slicing, split, isdigit and two counters, so a candidate is more likely to get this item right than either sibling.",
      "The sourceAnchor \"Programming for Everybody - lists and tuples with loops and conditional code\" names tuples, which docs/plan/course-syllabi.md does not list for PY-1 (it lists strings, lists and dictionaries), so the anchor cites a topic outside the recorded syllabus."
     ],
     "fix": "Raise the trace effort to match the siblings by adding one more step that must be reasoned through, for example a fourth tuple and a second accumulator: results = [(\\\"login\\\", True), (\\\"search\\\", False), (\\\"export\\\", False), (\\\"report\\\", True)]; failed = []; passed_count = 0; for name, passed in results: if passed: passed_count += 1 else: failed.append(name); print(passed_count, len(failed), failed[-1]) with key \\\"2 2 export\\\" and distractors \\\"2 2 search\\\", \\\"4 0 report\\\" (ignores condition) and \\\"1 2 export\\\" (miscount); update the rationale accordingly. Also change the sourceAnchor to \\\"Programming for Everybody - lists with loops and conditional code (reading a short program end to end)\\\" so it names only topics recorded in docs/plan/course-syllabi.md."
    }
   ]
  },
  {
   "item": "python-07-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Calibration: python-07-a tests only the single rule 'names start with test_' (key: `def test_totals():` and `def test_empty_input():`), whereas sibling python-07-b requires knowing both file patterns (`login_test.py` collected) and the conftest.py exception, and python-07-c requires the additional class rule (`CartChecks.test_remove_item` not collected), so a candidate who knows just the function prefix rule is more likely to score on 07-a than on either sibling, which breaks slot interchangeability."
     ],
     "fix": "Equalise the slot in one of two directions. Either (preferred, keeps 07-a at the docs/04 exemplar bar) simplify the siblings to the same one-rule depth: in 07-b drop conftest.py and login_test.py in favour of plain non-matching names so the item turns only on the test_*.py file prefix, and in 07-c drop the CartChecks class so the item turns only on the test_ prefix at module and TestCart level; or raise 07-a by one nuance to match the siblings, for example adding a sixth option `def Test_totals():` or a module-level `def testTotals():` so the item also tests that the prefix must be exactly `test_`, and extend the rationale accordingly. The item itself is otherwise sound: on-topic, generic, read-and-predict, structurally valid, one defensible key set, plausible distractors, complete rationale and anchor."
    }
   ]
  },
  {
   "item": "python-07-c",
   "revised": 1,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Difficulty is a step above sibling python-07-a: the distractor \"CartChecks.test_remove_item\" requires a second discovery rule (classes must be named Test*) that sibling a never tests, so a candidate who knows only the test_ function prefix would get a right but c wrong; c is calibrated closer to b than to a.",
      "The rationale states \"pytest collects test_-prefixed functions at module level and test_-prefixed methods\", but pytest's default python_functions prefix is \"test\" (an underscore is not required), so the dispute record is imprecise even though no option in this item exposes the difference."
     ],
     "fix": "Either accept that a is the lighter sibling and lift a to the same two-rule level (add a Test* class rule there too), or keep c at one rule by renaming CartChecks to TestCartChecks so that its test_ method is collected and the item then has 3 correct options (a, c, d) resting only on the function-prefix rule. Separately, reword the rationale to \"pytest collects functions and methods whose names start with test (the test_ prefix is the conventional form) inside modules and inside classes whose names start with Test\" so the dispute record matches pytest's actual default."
    }
   ]
  },
  {
   "item": "python-07-b",
   "revised": 1,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Difficulty is markedly above its siblings: the key set depends on knowing the suffix file pattern behind \"login_test.py\" (\"By default pytest collects files named test_*.py or *_test.py\"), whereas python-07-a and python-07-c both resolve from the test_ prefix rule alone, so a candidate who holds only the prefix rule scores 1 on a and c but 0 on b under all-or-nothing multi scoring; python-07-a even trains that candidate to reject the suffix form via its \"def totals_test():\" distractor.",
      "The option \"conftest.py\" under a stem saying \"Each file defines a function named `test_one` that contains an assert\" is a contrived scenario that tests a reserved-file exception rather than the naming convention the slot targets, pushing the item towards tool trivia instead of pattern reading.",
      "Two distractors, \"testlogin.py\" and \"login_tests.py\", differ from the keys by a single character (a missing underscore, an extra s), so the item rewards character-level pattern spotting more than its siblings do and reads as trick wording."
     ],
     "fix": "Rebuild the option set so the item turns on the same test_ prefix rule as its siblings and keep 5 options with 2 correct: keys test_login.py and test_logout.py; distractors check_login.py, login_helpers.py (replacing conftest.py) and login_tests.py (keep one near-miss at most, drop testlogin.py). Update the rationale to explain that pytest matches files by the test_ prefix and that the three others do not carry it, and note login_tests.py does not end in _test.py. Keep the sourceAnchor unchanged."
    }
   ]
  },
  {
   "item": "python-08-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Charter compliance",
     "problems": [
      "Option d, \"The test is skipped, because the assert compares two string values rather than numbers.\", is closer to absurd filler than a plausible wrong answer: no mental model of pytest links an assert's operand types to a skip outcome, so it violates the charter rule that distractors must be wrong answers a genuinely unsure person might pick (severity: minor)."
     ],
     "fix": "Replace option d with a distractor grounded in a real misconception about assert, for example: \"The test passes, because the assert only checks that get_status(7) returned a non-empty value.\" and update the rationale's last sentence to: \"An assert with == evaluates the whole comparison, not merely whether a value was returned, so a non-empty return value does not make the test pass.\""
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The discriminating knowledge in python-08-a is Python core string comparison rather than pytest assert semantics: the key \"The test fails, because \\\"Shipped\\\" and \\\"shipped\\\" are different strings and the assert compares them exactly.\" is answerable by anyone who knows == is case-sensitive, whereas sibling python-08-b requires knowing that a failing assert stops the test function and that later asserts do not run, so the two items are not interchangeable in difficulty or in what they test (severity: major).",
      "Distractor d, \"The test is skipped, because the assert compares two string values rather than numbers.\", is not a belief a genuinely unsure candidate would hold, so python-08-a effectively offers three live options while python-08-b offers four plausible ones, making a markedly easier than its sibling (severity: minor)."
     ],
     "fix": "Rewrite python-08-a so the fact being tested is assert and failure semantics rather than string case, matching python-08-b in kind: for example show a test with a plainly false assert followed by a further statement, e.g. `def test_status():\\n    status = get_status(7)\\n    assert status == \\\"Delivered\\\"\\n    print(\\\"checked\\\")` where get_status returns \\\"Shipped\\\", and ask what pytest reports, with key \\\"The test fails at the assert, pytest shows the two compared values, and the print line does not run\\\" and distractors \\\"The test fails but the print line still runs\\\", \\\"The test passes because get_status returned a value without raising an exception\\\", \\\"The test errors because get_status has no test_ prefix\\\". Whatever form is chosen, replace distractor d with a plausible misconception such as \\\"The test passes, because get_status returns a value without raising an exception.\\\" and update the rationale to explain why each alternative is wrong."
    }
   ]
  },
  {
   "item": "python-08-b",
   "revised": 1,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "The stem asks \"What does pytest report when it runs this test?\" but the key \"the last assert is not executed\" and distractor \"the last assert still runs and passes\" describe execution behaviour rather than anything pytest literally reports, so the question and the options are framed on slightly different things (minor)."
     ],
     "fix": "Change the stem to \"What happens when pytest runs this test?\" (matching the framing of the options), and leave the options, key, rationale and sourceAnchor unchanged; the key was verified by running the test under pytest (one failure, assert 3 == 4, third assert never reached)."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "The stem asks \"What does pytest report when it runs this test?\" but the clause that distinguishes the key from option b, \"the last assert is not executed\", describes execution behaviour that pytest's report does not itself state, so the stem wording does not quite match what the options ask the candidate to decide."
     ],
     "fix": "Change the stem to \\\"What happens when pytest runs this test?\\\" so that the options, which describe where the test fails and whether the remaining assert runs, answer exactly what the stem asks; no other change is needed."
    }
   ]
  },
  {
   "item": "python-09-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "The stem asks \"What does the fixture do when pytest runs `test_new_order_has_no_lines`?\" but the key and every distractor describe what pytest or the decorator does (\"pytest calls order()...\", \"pytest runs the test once...\", \"The test fails...\", \"The decorator marks order as a test...\"), so the question wording does not match the form of the answers.",
      "The key text \"pytest calls order() before the test\" reads as if the decorated name is called directly, which pytest actively forbids (\"Fixtures are not meant to be called directly\"); the intended meaning is that pytest runs the fixture function, and a pedantic reader could find the wording imprecise even though the key remains the only correct option."
     ],
     "fix": "Change the stem question to \"What happens when pytest runs `test_new_order_has_no_lines`?\" so all four options answer it in the same form, and reword option (a) to \"pytest runs the order fixture function before the test and passes the returned dictionary into the test through the order parameter.\" No change to the key, distractors, rationale or anchor is needed."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "The stem asks \"What does the fixture do when pytest runs `test_new_order_has_no_lines`?\" but options c (\"The test fails with a NameError, because order is used as a variable without being assigned.\") and d (\"The decorator marks order as a test, so it is collected and run alongside test_new_order_has_no_lines.\") answer a different question (what happens to the test, what the decorator does), so the options are not parallel and option a is the only one phrased as an action the fixture performs, a slight cue towards the key."
     ],
     "fix": "Reword the stem to a neutral outcome question that every option answers on equal terms, for example: \"A test file contains the code below. What happens when pytest runs `test_new_order_has_no_lines`?\" and keep the four options unchanged; no change to key, rationale or sourceAnchor is needed. File: C:/Projects/Test Automation Cap Build/bank/drafts/python/python-09-a.json."
    }
   ]
  },
  {
   "item": "python-10-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Charter compliance",
     "problems": [
      "[major] Keyed option c, \"Compare amounts within an agreed tolerance rather than by exact equality.\", is disputable because the stem only says the amount is \"stored to two decimal places in the source and as a floating point number in the target\" and never establishes that an agreed tolerance exists or that target values carry rounding differences; a competent professional could argue that rounding the target value to two decimal places and comparing exactly is a trustworthy alternative, so tolerance is not something the comparison \"must include\" and the key set is not beyond argument (the sibling python-10-b avoids this by stating \"the agreed tolerance\" in its stem).",
      "[minor] The stem asks \"Which checks must the comparison include\" but keyed options a and c describe comparison methods (\"Match rows by id rather than by list position\", \"Compare amounts within an agreed tolerance\") rather than checks, and distractor e (\"Treat the extracts as matching when both contain the same number of rows\") is a conclusion rather than a check, so the options are not parallel with the stem's noun.",
      "[minor] Options a, d and f embed their own justification (\"because the two queries give no guarantee of the same row order\", \"so that the failure report stays short\", \"so that differences in type are ignored\"), which makes the keyed option a self-cueing and the distractors self-refuting; options b, c and e carry no such clause, so the option set is uneven in style."
     ],
     "fix": "Add one sentence to the stem establishing the tolerance as a given, for example: \\\"The team has agreed that amounts differing by less than 0.005 are equivalent.\\\" Then reword the stem to \\\"Which of these must the comparison do for its result to be trustworthy?\\\" so that every option is a behaviour of the comparison, and strip the trailing justification clauses so all six options are parallel: a \\\"Match rows by id rather than by list position.\\\"; b \\\"Report every id that appears in one extract and not in the other.\\\"; c \\\"Compare amounts within the agreed tolerance rather than by exact equality.\\\"; d \\\"Stop at the first mismatching row.\\\"; e \\\"Treat the extracts as matching whenever both contain the same number of rows.\\\"; f \\\"Convert every value to a string before comparing.\\\" Keep the existing rationale, which already covers all six options."
    },
    {
     "lens": "Key defensibility",
     "problems": [
      "Option c \"Compare amounts within an agreed tolerance rather than by exact equality.\" is contestable as a \"must\": the stem only says the amount is \"stored to two decimal places in the source and as a floating point number in the target\", and a directly loaded 2 dp value round-trips exactly (float(Decimal('12.34')) == 12.34 is True, round(12.34, 2) == 12.34 is True), so a competent professional can argue the trustworthy check is to normalise both sides to two decimal places (or to Decimal) and compare exactly, with no tolerance required, and would then omit c and lose the mark.",
      "Minor: the stem \"Which checks must the comparison include for its result to be trustworthy?\" is a judgement-of-necessity stem while options a and c describe matching strategies rather than checks, and the sibling python-10-b is a read-the-code-and-predict item, so the two items in the slot differ in cognitive style."
     ],
     "fix": "Make the need for a rounding allowance unavoidable in the stem and widen option c to cover both rounding and tolerance. For example, change the stem's amount sentence to: \"The target `amount` is produced by floating point arithmetic during the load, so a value that agrees with the source to two decimal places can differ from it by a rounding error far smaller than one penny.\" Reword option c to: \"Compare amounts with an allowance for floating point rounding, for example a small tolerance or rounding to two decimal places, rather than by exact equality of the raw values.\" Update the rationale to say that exact equality of raw values reports false mismatches on rounding errors and that either a tolerance or normalisation to two decimal places removes them. Optionally rephrase the stem's question as \"Which of these must the comparison do for its result to be trustworthy?\" so that strategies and checks both fit."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Option c reads \"Compare amounts within an agreed tolerance rather than by exact equality.\" but the stem never states that a tolerance has been agreed, so a candidate who would round the target's floating point amount to two decimal places and compare exactly can argue that a tolerance is not a check the comparison \"must\" include, whereas sibling python-10-b anchors the same concept with \"the agreed tolerance\" in its stem."
     ],
     "fix": "Add one sentence to the stem after the sentence about the amount column, for example: \"The team has agreed that amounts differing by less than 0.005 are to be treated as equal.\" Then change option c to \"Compare amounts within the agreed tolerance rather than by exact equality.\" so that the key follows from facts on the page, matching the framing used in python-10-b."
    }
   ]
  },
  {
   "item": "python-10-b",
   "revised": 1,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "Option a, \"Both lists hold the same rows, but in a different order.\", is keyed as a false mismatch, but the stem (\"Two systems each return a list of dictionaries, one row per customer with the keys `id` and `balance`\") never states that row order is not significant to the agreed equivalence, so a candidate can argue that two lists in different order are not \"equivalent\" data and that only b is the key; the sibling python-10-a avoids this by stating \"the two queries give no guarantee of the same row order.\""
     ],
     "fix": "Add one sentence to the stem making the equivalence contract explicit, for example after the first sentence: \\\"Rows are identified by `id`, the order of rows is not significant, and balances are considered equal when they differ by no more than an agreed tolerance.\\\" Then keep the key set {a, b}, the options and the rationale unchanged."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "Minor: the stem refers to \"the agreed tolerance\" (\"even though the data is equivalent within the agreed tolerance\") before any tolerance has been introduced, so the definite article points at something the stem never states; the key does not depend on its value, but the sentence is not plain on first reading.",
      "Minor: the stem formats the keys inconsistently, writing \"the keys `id` and `balance`\" in code font and then \"where balance is a floating point number\" in plain text."
     ],
     "fix": "In C:/Projects/Test Automation Cap Build/bank/drafts/python/python-10-b.json change the stem's first sentence to \"Two systems each return a list of dictionaries, one row per customer with the keys `id` and `balance`, where `balance` is a floating point number and the team has agreed a tolerance for comparing balances.\" and keep the closing question as \"In which of these situations does the function report a mismatch even though the data is equivalent within the agreed tolerance?\" so the tolerance is introduced before it is referred to and the key name is consistently in code font."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Minor: the stem qualifier \"even though the data is equivalent within the agreed tolerance\" lets a candidate discard options c (\"The target list has one row fewer than the source list.\") and d (\"differs from the source by more than the agreed tolerance\") from the wording alone, without reading the function, so the live decision collapses to three options and the item is noticeably easier than python-10-a, where all six options require judgement.",
      "Minor: the blueprint facet \"missing rows\" is present only as the stem-excluded distractor \"The target list has one row fewer than the source list.\", so unlike python-10-a (which keys \"Report every id that appears in one extract and not in the other\") this item never tests whether the candidate knows a comparison must surface rows present on one side only, weakening topic interchangeability within the slot.",
      "Minor: the distractor \"Both lists are empty.\" is weak filler that a genuinely unsure person is unlikely to pick as a false mismatch, so it does not pull its weight against the two keys."
     ],
     "fix": "Keep the shown function and reframe the question so every option requires reasoning about what `==` does, for example: \"For which of these situations does the function return False?\" with six options and three keys: (a) same rows in a different order [True/False key], (b) a balance differs only by a floating point rounding error smaller than the agreed tolerance [key], (c) the target list has one row fewer than the source list [key], (d) the dictionaries in one list were built with the keys in the order balance then id rather than id then balance [not a key, dict equality ignores key order], (e) a balance is stored as 10.0 in one list and 10 in the other [not a key, 10.0 == 10 is True], (f) both lists are empty [not a key]. This restores the missing-rows facet, removes the stem giveaway, replaces the weak empty-list distractor with two plausible equality traps, and brings the item to the same six-option, three-key difficulty as python-10-a. Update the rationale to explain each of the six outcomes."
    }
   ]
  },
  {
   "item": "sql-01-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Charter compliance",
     "problems": [
      "The stem carries two visual elements, a five-row pipe table of `products` followed by a fenced block \"```sql\\nSELECT name\\nFROM products\\nWHERE category = 'furniture' AND price < 100;\\n```\", which exceeds the charter's limit of one visual element per item; the only permitted exception is two tiny tables for a join, and the docs/04 SQL slot 2 exemplar shows the intended form, with the tables shown and the query written inline in the question sentence."
     ],
     "fix": "Keep the products table as the single visual element and fold the one-line query into the question sentence as inline code, following the docs/04 SQL slot 2 exemplar, for example: \\\"Which names does `SELECT name FROM products WHERE category = 'furniture' AND price < 100;` return?\\\". Apply the same change to sibling sql-01-b so the slot stays interchangeable. Everything else checks out: id sql-01-a, assessment sql, section foundations, slot 1, type single, exactly 4 options with ids a to d and exactly 1 correct, fixedOrder false (non-numeric options), difficulty standard, no negative or opinion stem, no banned constructs, no names, UK English, table of exactly 5 rows, plausible non-synonymous distractors, rationale covering every alternative, sourceAnchor matching Intermediate SQL chapter 3 (data filtering: multiple conditions), and the key confirmed by running the query in PGlite (Lamp, Chair)."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "The query `SELECT name FROM products WHERE category = 'furniture' AND price < 100;` is presented as inline backticked code rather than in a fenced ```sql block, which sits in tension with the charter line \"Code in fenced blocks\" (it does match the docs/04 slot 2 exemplar pattern of an inline query beside a table, and keeps to one visual element, so this is a style inconsistency with sibling sql-01-b rather than a defect that a candidate could dispute)."
     ],
     "fix": "Either leave as is (it follows the binding docs/04 SQL slot 2 exemplar, which shows the query inline alongside a table, and adding a fenced block would create a second visual element), or for consistency with sql-01-b describe the five rows in prose as (id, name, category, price) tuples and put the query in a fenced ```sql block, keeping exactly one visual element. Everything else verified: 4 options with exactly 1 correct, ids a-d, type single, section foundations, slot 1, difficulty standard, fixedOrder false (non-numeric options), table of 5 data rows, no negative or opinion stem, no banned constructs, no names or emojis, UK English, rationale covers every alternative, sourceAnchor matches Intermediate SQL chapter 3 \"data filtering: multiple conditions\" in docs/plan/course-syllabi.md, and PGlite confirms the key returns Lamp and Chair while the three distractors match the rationale's stated wrong queries exactly."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "[minor] The query is presented inline in the stem as `SELECT name FROM products WHERE category = 'furniture' AND price < 100;` rather than in a fenced ```sql block as the authoring rules require and as sibling sql-01-b does, so the two siblings differ in presentation form (table plus inline query versus prose rows plus fenced query) even though they are otherwise interchangeable.",
      "[minor] Sibling sql-01-b tests a boundary value (\"guests >= 2\" with a row holding exactly 2 guests, and distractor d \"3\" exploiting it), whereas sql-01-a has no price at or near 100 and its distractor d \"Desk\" (reading `<` as `>`) is a weaker lure, making sql-01-a marginally easier than its sibling."
     ],
     "fix": "Rewrite sql-01-a to match sql-01-b's form and difficulty while keeping the one-visual-element rule: describe the rows in prose (\\\"The table `products` has four columns: `id`, `name`, `category` and `price`. It holds five rows, written as (id, name, category, price): (1, Pen, stationery, 2), (2, Desk, furniture, 150), (3, Lamp, furniture, 100), (4, Chair, furniture, 85), (5, Pad, stationery, 5).\\\") and put the query in a fenced ```sql block. Setting Lamp's price to 100 with the condition `price < 100` introduces a boundary case equivalent to the sibling's: key becomes \\\"Chair\\\", with distractors \\\"Lamp and Chair\\\" (treats < as <=), \\\"Desk, Lamp and Chair\\\" (ignores the price condition) and \\\"Pen, Chair and Pad\\\" (ignores the category condition). Update the rationale accordingly and re-verify by running the query and each distractor's query."
    }
   ]
  },
  {
   "item": "sql-01-b",
   "revised": 1,
   "fails": [
    {
     "lens": "Charter compliance",
     "problems": [
      "The stem of bank/drafts/sql/sql-01-b.json contains two visual elements, a pipe table (\"| id | room | guests | status |\") and a fenced code block (\"```sql\\nSELECT id\\nFROM bookings ...\"), which breaks the charter rule \"one visual element per item at most\"; the sibling bank/drafts/sql/sql-01-a.json has the same table-plus-code shape, so the whole slot needs one consistent treatment."
     ],
     "fix": "Keep the single fenced ```sql block and present the rows inline in prose in the style of the docs/04 slot 2 exemplar, for example: \"The table bookings holds these rows as (id, room, guests, status): (1, 101, 2, confirmed), (2, 102, 4, cancelled), (3, 103, 3, confirmed), (4, 104, 1, confirmed), (5, 105, 5, pending).\" Apply the same change to sql-01-a. Alternatively, if the owner decides that a shown table plus its query counts as one composite visual for SQL slots 1 to 6, record that decision in docs/04 so the rule and the items agree; the key, options, rationale and anchor need no change either way."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The rows are shown as positional prose tuples, \"written as (id, guests, status): (1, 2, confirmed), (2, 4, cancelled), ...\", rather than the \"shown table\" the slot 1 blueprint names, and sibling sql-01-a presents its data as a pipe table, so this item carries extra parsing load (mapping each tuple position back to a column name) that its sibling does not, which makes the two items less than fully interchangeable in reading difficulty. Severity: minor."
     ],
     "fix": "Match sql-01-a's presentation: show the five bookings rows as a pipe table (columns id, guests, status) and put the query inline in backticks in the final sentence, for example \"Which ids does `SELECT id FROM bookings WHERE status = 'confirmed' AND guests >= 2;` return?\", dropping the fenced code block so the item still has exactly one visual element. Keep the options, key, rationale and sourceAnchor unchanged."
    }
   ]
  },
  {
   "item": "sql-02-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Charter compliance",
     "problems": [
      "The stem of C:/Projects/Test Automation Cap Build/bank/drafts/sql/sql-02-a.json contains three visual elements (the two pipe tables \"| customers.id |\" and \"| orders.customer_id |\" plus the fenced block \"```sql\\nSELECT *\\nFROM customers\\nINNER JOIN orders ON customers.id = orders.customer_id;\\n```\"), which exceeds the charter limit of one visual element per item, or two tiny tables only for a join item in the form the docs/04 exemplar shows, where the query is written inline in the question sentence rather than as a separate code block."
     ],
     "fix": "Follow the docs/04 SQL slot 2 exemplar exactly: keep the two tiny tables as the single permitted visual element and move the short query inline into the question sentence, for example: \\\"How many rows does `SELECT * FROM customers INNER JOIN orders ON customers.id = orders.customer_id` return?\\\", removing the separate fenced block. Apply the same change to the siblings sql-02-b.json and sql-02-c.json so the slot stays interchangeable. Everything else checks out: id, assessment, section, slot, type, four options with one correct, ids a to d, fixedOrder true with numeric options ascending, difficulty standard, tables of at most 5 rows, no banned constructs, UK English, distractors plausible and each explained in the rationale, key verified as 3 rows by running the query in PGlite, and sourceAnchor matching \\\"Joining Data in SQL - combining data horizontally ... INNER JOIN\\\" in docs/plan/course-syllabi.md."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "sql-02-a and sibling sql-02-c are the same scenario with cosmetic changes: both are `INNER JOIN` between a 3-row left table and a right table that carries duplicate keys, one unmatched left row and one unmatched right row (a: customers 1,2,3 / orders 1,1,3,4; c: departments 1,2,3 / staff 2,2,3,3,5), and both use the same distractor architecture where the key-plus-one option is the right-table row count and also the LEFT JOIN count, and the key-plus-two option is the full outer join count, so two of the three items in this slot test one identical pattern while only sql-02-b tests LEFT JOIN.",
      "The rationale for sql-02-a explains option c (\"4\") only as \"counts every order row, including the unmatched order for customer 4\" and omits that 4 is also the LEFT JOIN row count (customer 2 with NULLs plus the 3 matched rows), which is the INNER-versus-LEFT confusion this slot exists to detect and is the most likely reason a candidate picks it; sibling sql-02-c's rationale does state this equivalence."
     ],
     "fix": "Keep sql-02-a unchanged as it reproduces the docs/04 calibration exemplar verbatim, and instead rewrite sql-02-c so the slot has three distinct patterns: for example an INNER JOIN where the duplicate keys sit in the left table rather than the right (so the key-plus-one distractor is no longer simply the right-table row count), or a second LEFT JOIN item whose unmatched rows are on the left only, keeping 3 to 5 rows per table and numeric options in ascending order. In sql-02-a's rationale, extend the sentence on option c to: \\\"Option c counts every order row, including the unmatched order for customer 4; it is also the row count a LEFT JOIN would give, because that join keeps customer 2 with NULL order columns.\\\""
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The item reproduces the docs/04 slot 2 exemplar verbatim (customers 1, 2, 3; orders 1, 1, 3, 4; INNER JOIN; options 2, 3, 4, 5; key 3), so the calibration bar and a live bank item are the same question rather than the item being calibrated against it; minor.",
      "Within the slot, item a asks only for the INNER JOIN count (matched pairs) while siblings b and c ask for a LEFT JOIN count (matched pairs plus unmatched left rows), which is one reasoning step more, so a is marginally the easiest of the three; minor.",
      "The query in the stem, \"SELECT * FROM customers INNER JOIN orders ON customers.id = orders.customer_id\", has no trailing semicolon whereas both siblings end their query with \";\"; minor."
     ],
     "fix": "Keep the structure but vary the data so the item is a calibrated sibling of the exemplar rather than a copy, and give it a small second step to match b and c, for example: customers.id 1, 2, 3, 4 and orders.customer_id 2, 2, 4, 5 with the question \\\"How many rows does SELECT * FROM customers INNER JOIN orders ON customers.id = orders.customer_id; return?\\\" (key 3; distractors 2 = distinct matched customers, 4 = every order row, 5 = LEFT JOIN count keeping customers 1 and 3), and add the trailing semicolon so all three siblings present the query identically. Update the rationale to match the new rows."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "The query in the stem of bank/drafts/sql/sql-02-a.json ends without a semicolon (`... ON customers.id = orders.customer_id`) while both siblings sql-02-b and sql-02-c end their queries with one (`... = tickets.employee_id;` and `... = departments.id;`), so items in the same slot present SQL in two different styles."
     ],
     "fix": "Make the three sql-02 items consistent: either append a semicolon in sql-02-a so the stem reads `SELECT * FROM customers INNER JOIN orders ON customers.id = orders.customer_id;`, or remove the trailing semicolons from sql-02-b and sql-02-c. Nothing else in sql-02-a needs to change: the key of 3 rows is verified by execution, the structure, wording, tables, rationale and sourceAnchor all comply with the charter."
    }
   ]
  },
  {
   "item": "sql-02-b",
   "revised": 1,
   "fails": [
    {
     "lens": "Charter compliance",
     "problems": [
      "The stem carries three visual elements (the `employees.id` pipe table, the `tickets.employee_id` pipe table and the fenced ```sql block starting \"SELECT *\\nFROM employees\\nLEFT JOIN tickets\"), which exceeds the charter's \"one visual element per item at most\" and the join allowance of two tiny tables only; the docs/04 slot 2 exemplar keeps the query in the prose of the question sentence rather than in a third block. Severity: minor (style only; the item is otherwise correct, the key of 5 rows, the INNER JOIN distractor of 3 and the full outer join distractor of 6 were verified against PGlite, and the layout is identical in sql-02-a and sql-02-c, so the item cannot be disputed by a candidate)."
     ],
     "fix": "Keep the two tiny tables and fold the query into the question sentence as inline code, matching the docs/04 slot 2 exemplar, for example: \"How many rows does `SELECT * FROM employees LEFT JOIN tickets ON employees.id = tickets.employee_id;` return?\" Apply the same change to sql-02-a and sql-02-c so the slot stays uniform. If the parent decides that the \"code in fenced blocks\" rule overrides the one-visual-element rule for join items, record that decision once and leave all three slot 2 items as they are."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Minor: sql-02-b and sql-02-c share an identical option set (\"3\", \"4\", \"5\", \"6\") and the identical key \"5\" for the same LEFT JOIN concept, so two of the three interchangeable items in the slot converge on one answer value and the slot carries only one INNER JOIN item against the blueprint \"INNER vs LEFT JOIN\"; the reasoning shapes differ (unmatched left rows kept versus the many-side placed on the left) so this is not a full duplicate, but the overlap weakens the independence of the slot.",
      "Minor: the query in the stem ends with a semicolon, \"SELECT * FROM employees LEFT JOIN tickets ON employees.id = tickets.employee_id;\", whereas sibling sql-02-a presents its query without one, a cosmetic inconsistency across items that are meant to be interchangeable."
     ],
     "fix": "Reshape sql-02-b's data so its key differs from sql-02-c's, for example employees(id: 10, 20, 30) and tickets(employee_id: 10, 10, 30, 50), which makes the LEFT JOIN return 4 rows; offer options 3 (the INNER JOIN count), 4 (key), 5 (a full outer join count) and 7 (every row of both tables added together), keep fixedOrder true with the options ascending, update the rationale accordingly, and drop the trailing semicolon from the inline query to match sql-02-a."
    }
   ]
  },
  {
   "item": "sql-02-c",
   "revised": 1,
   "fails": [
    {
     "lens": "Charter compliance",
     "problems": [
      "The stem contains three visual elements (the `| departments.id |` table, the `| staff.dept_id |` table and the fenced ```sql block), which exceeds the charter rule of one visual element per item at most, with the join exemplar in docs/04 allowing only the two tiny tables and stating the query inline in prose; siblings sql-02-a and sql-02-b share the same structure (severity: minor)."
     ],
     "fix": "Keep the two tiny tables and drop the fenced block by stating the query inline in the question sentence, as the slot 2 exemplar does, for example: \"How many rows does `SELECT * FROM departments INNER JOIN staff ON departments.id = staff.dept_id` return?\". Apply the same change to sql-02-a and sql-02-b so the three items in the slot stay interchangeable."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "sql-02-c duplicates sql-02-a with cosmetic changes only: both stems use an INNER JOIN over a one-side table with one unmatched row and a many-side table with duplicate keys and one orphan (\"SELECT *\\nFROM departments\\nINNER JOIN staff ON departments.id = staff.dept_id;\" versus \"SELECT *\\nFROM customers\\nINNER JOIN orders ON customers.id = orders.customer_id;\"), and the distractor logic is identical in both rationales (count of left rows, count of right rows, full outer join count), so in a contested slot whose blueprint is \"INNER vs LEFT JOIN\" the third item adds no distinct coverage while sql-02-b alone carries the LEFT JOIN half."
     ],
     "fix": "Keep the same two shown tables (departments.id 1, 2, 3 and staff.dept_id 2, 2, 3, 3, 5) but change the query to a LEFT JOIN with the many-side table on the left: \"SELECT *\\nFROM staff\\nLEFT JOIN departments ON staff.dept_id = departments.id;\". Verified in PGlite the key becomes 5 (every staff row kept, the dept_id 5 row with NULL department columns); keep fixedOrder true with ascending options 3, 4, 5, 6 where 3 is the department count, 4 is the INNER JOIN count and 6 is the full outer join count. Rewrite the rationale accordingly (why 5 is right: LEFT JOIN preserves all rows of the left table, staff; why 4 is wrong: it drops the unmatched staff row as an INNER JOIN would; why 3 is wrong: it counts departments, not result rows; why 6 is wrong: it also keeps department 1, which only a full outer join would do) and change the sourceAnchor to \"Joining Data in SQL - combining data horizontally: LEFT JOIN\". This keeps the item at the same length and difficulty as its siblings while giving the slot one INNER item, one LEFT item with the one-side on the left, and one LEFT item with the many-side on the left, so the three are interchangeable yet genuinely distinct."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Minor: sql-02-c and its sibling sql-02-b share the identical fixed option list \"3\", \"4\", \"5\", \"6\" and the identical key \"5\" (option c in both), so two of the three items in the slot resolve to the same displayed answer, which weakens the intended variation between papers and retakes after a reset.",
      "Minor: because departments.id is unique and staff is the left table, the key for \"SELECT * FROM staff LEFT JOIN departments ON staff.dept_id = departments.id\" equals the left table's own row count (5), so a candidate who only counts the staff rows scores without reasoning about matching, whereas sql-02-a and sql-02-b both defeat that shortcut (customers count 3 and employees count 4 are wrong answers there), making sql-02-c slightly more forgiving than its siblings."
     ],
     "fix": "Change the staff rows to dept_id 2, 3, 5, 5 (departments unchanged at 1, 2, 3). The LEFT JOIN then returns 4 rows, INNER JOIN 2, FULL JOIN 5 and the departments count is 3, so the options become 2, 3, 4, 5 ascending with key 4, which separates the item's option set and key from sql-02-b. Update the rationale accordingly: option a (2) is the INNER JOIN count that drops both unmatched staff rows for department 5, option b (3) counts the department rows, option d (5) adds a row for unmatched department 1 as a full outer join would. The left-count shortcut is inherent to any child-on-the-left LEFT JOIN over a unique parent key; if the panel wants it removed, replace this item with an INNER JOIN variant on the new data (key 2, options 2, 3, 4, 5) instead, which also balances the slot to two INNER and one LEFT item."
    }
   ]
  },
  {
   "item": "sql-03-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "The key is confirmed correct by execution in PGlite (Ann 100, Ben 55) and no alternative is defensible, but the stem's customer values \"Ann\", \"Ben\" and \"Cara\" are person names, which the absolute rule \"no client, company, product or person names\" forbids, and the sibling sql-03-b avoids names entirely by grouping on regions."
     ],
     "fix": "Replace the customer column with a non-personal grouping value, for example rename the column to `branch` with values North, South and West (or `customer_code` with values C1, C2, C3), keeping the same amounts so the totals stay 100, 55 and 30, and update the four option texts and the rationale to use the new labels."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The customer column holds first names (\"| 1 | Ann | 40 |\", \"| 2 | Ben | 35 |\", \"| 4 | Cara | 30 |\") and the options repeat them (\"Ann with total 100 and Ben with total 55\"), which brushes against the rule that items carry no person names; the sibling sql-03-b avoids this by grouping on region values (North, South, East), so the two items are not equally generic."
     ],
     "fix": "Replace the person-name values with neutral category values while keeping every number and the option structure unchanged, for example rename the column to `category` and use stationery (rows 1 and 3, total 100), furniture (rows 2 and 5, total 55) and lighting (row 4, total 30), then update the query, the options and the rationale to match. Everything else checks out: the item is on the slot 3 blueprint topic (GROUP BY with HAVING, read-and-predict), the key is uniquely correct (Ann 100, Ben 55 survive HAVING SUM(amount) > 50; Cara 30 does not), the three distractors map to the same misconceptions as the sibling's (HAVING ignored, aggregate confusion or WHERE-before-grouping, one qualifying group dropped), and table size, query length and arithmetic load are interchangeable with sql-03-b."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "The stem contains two visual elements, a pipe table (\"| id | customer | amount |\") and a fenced code block (\"```sql\\nSELECT customer, SUM(amount) AS total ...\"), but the charter allows one visual element per item at most and the SQL slot 2 exemplar keeps the table as the only visual by writing the query inline in the question sentence (the sibling sql-03-b has the same defect).",
      "The table uses person names as data values (\"Ann\", \"Ben\", \"Cara\"), which conflicts with the rule of no client, company, product or person names; the exemplar uses ids only and the sibling sql-03-b uses region values."
     ],
     "fix": "Keep the pipe table as the single visual element and move the query inline into the question sentence in the exemplar's style, for example: \"Which rows does `SELECT customer, SUM(amount) AS total FROM orders GROUP BY customer HAVING SUM(amount) > 50` return?\" Replace the person names with neutral codes, for example a `customer` column holding C1, C2, C3 (Ann to C1, Ben to C2, Cara to C3), and update the four options and the rationale to use the same codes (key becomes \"C1 with total 100 and C2 with total 55\"). Apply the same two changes to sql-03-b so the slot stays interchangeable. Everything else checked out: 4 options with exactly 1 correct, ids a to d, section foundations and type single are right for slot 3, no negative or opinion stems, no all or none of the above, distractors are plausible and distinct from the key, the rationale covers all four options, the table has 5 rows, the sourceAnchor maps to Intermediate SQL chapter 1 data aggregation, and the key was verified against PGlite (Ann 100, Ben 55)."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Difficulty is slightly below the sibling sql-03-b: sql-03-a uses SUM with a strict `HAVING SUM(amount) > 50` where no group total sits on or near the threshold (100, 55, 30), whereas sql-03-b uses AVG with `HAVING AVG(amount) >= 80` and a group whose average is exactly 80, so the sibling demands both a division and a boundary judgement that this item never asks for (minor).",
      "Distractor d, \"stationery with total 100\", does not correspond to any recognisable misreading of GROUP BY or HAVING (furniture's total of 55 is clearly above 50), so it is weaker than the sibling's distractor d, which captures the plausible confusion of >= with > at an exact boundary (minor).",
      "The sourceAnchor \"Intermediate SQL - data aggregation: one grouping column and filtering groups with HAVING\" places HAVING under Chapter 1 data aggregation, but in the syllabus listed in docs/plan/course-syllabi.md filtering with HAVING sits under Chapter 3 data filtering (complex filtering); the sibling carries the same imprecision (minor)."
     ],
     "fix": "Equalise with the sibling by introducing a boundary case and replacing the weak distractor: change row 4 to `| 4 | lighting | 50 |` so the lighting total is exactly 50 and the strict > excludes it; replace option d with \"stationery with total 100, furniture with total 55 and lighting with total 50\" (correct: false) and update the rationale to say option d treats > as >= and keeps lighting, whose total of exactly 50 is not greater than 50. Option b then becomes the ignore-HAVING distractor with lighting 50 rather than 30 (edit its text accordingly), and option c stays as the WHERE-before-grouping distractor (only the 60 row exceeds 50, so \"stationery with total 60\"). Re-verify the query against the revised rows. In both sql-03-a and sql-03-b, tighten the sourceAnchor to \"Intermediate SQL - chapter 1 data aggregation (one grouping column) and chapter 3 data filtering (HAVING)\"."
    }
   ]
  },
  {
   "item": "sql-04-a",
   "revised": 1,
   "fails": [
    {
     "lens": "Charter compliance",
     "problems": [
      "The stem contains two visual elements, a pipe table (\"| id | name | price |\") followed by a fenced code block (\"```sql\\nSELECT name\\nFROM products\\nWHERE price > (SELECT AVG(price) FROM products);\\n```\"), which exceeds the charter limit of one visual element per item; note this is the bank-wide pattern for every predict-the-result SQL item (slots 1 to 6 and sql-10-b all pair one table with one code block), so it is a convention decision rather than a defect unique to sql-04-a."
     ],
     "fix": "Either record a bank-wide ruling that a shown table plus its query block counts as the exemplar's \"tables shown plus query\" pattern and is permitted for predict-the-result slots, or fold the query into the prose as the SQL slot 2 exemplar does: keep the table as the single visual element and change the question line to \"What does `SELECT name FROM products WHERE price > (SELECT AVG(price) FROM products);` return?\" with the same four options, rationale and anchor unchanged. Everything else in sql-04-a is verified: PGlite returns AVG 56.4 and the rows Desk and Chair, the four options have exactly one correct answer, the distractors map to distinct plausible misreadings (MAX instead of AVG, reversed comparison, inner result mistaken for outer result), and the rationale covers all four."
    }
   ]
  }
 ],
 "groupB": [
  {
   "item": "ta-03-b",
   "revised": 0,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "The stem phrase \"trying unusual sequences of actions to learn how it behaves and to find problems\" gives a candidate a small foothold to argue the activity is robustness or security probing (technology-facing, critique the product) rather than exploratory testing; the key d is still canonical, but the wording is unnecessary to reach it."
     ],
     "fix": "Rephrase the stem to keep the user-perspective, unscripted nature explicit and drop the robustness hint, for example: \"A tester spends an afternoon exploring a web shop's new returns screen without a script, working through it as a customer would and following different paths to learn how it behaves and to find problems. In the agile testing quadrants, which quadrant does this activity belong to?\" No change to options, key or rationale is needed."
    }
   ]
  },
  {
   "item": "ta-06-a",
   "revised": 0,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The key \"Checking whether the redesigned home page looks appealing to shoppers.\" is the most obvious textbook case of subjective judgement and can be identified by common sense without any course exposure, whereas sibling ta-06-b's key (\"a one-off migration ... after which its script is retired\") requires weighing a definite expected result against zero repeatability, so ta-06-a is somewhat easier than its sibling and the two are not fully interchangeable in difficulty."
     ],
     "fix": "In bank/drafts/ta/ta-06-a.json, make the key a less blatant no-oracle case so it sits at the same level of subtlety as ta-06-b, for example replace option a with \\\"Checking that the recommendation panel shows products a shopper would find interesting.\\\" and update the rationale to: \\\"Key a: whether a shopper would find the recommended products interesting is a matter of human judgement with no definite expected result for a script to compare against, so it is the weakest candidate.\\\" Leave the three distractors and the sourceAnchor unchanged."
    }
   ]
  },
  {
   "item": "sql-03-b",
   "revised": 0,
   "fails": [
    {
     "lens": "Charter compliance",
     "problems": [
      "The stem contains two visual elements, a five-row pipe table (\"| id | region | amount |\") and a fenced code block (\"```sql\\nSELECT region, COUNT(*) AS sales_made ...\"), which exceeds the charter limit of \"one visual element per item at most\" and the review rule of \"at most one visual element (one code block or one table ...) per item\"; the sibling sql-03-a has the identical structure, so any fix must be applied to both to keep the slot interchangeable. Severity: minor.",
      "Distractor d \"North with 2\" has no clear misconception path: a candidate who ignores HAVING gets option b, one who confuses COUNT with SUM gets option c, and one who misreads >= as > gets no rows at all, so nothing in the query leads an unsure person to drop South alone and the option functions as filler rather than a plausible wrong answer. Severity: minor."
     ],
     "fix": "1. Reduce the stem to a single visual element in the way the SQL slot 2 exemplar does: keep the pipe table and place the query inline in the question sentence, for example \"Which rows does `SELECT region, COUNT(*) AS sales_made FROM sales GROUP BY region HAVING COUNT(*) >= 2` return?\". If the parent decides that predict-the-result SQL items are exempt because they inherently need both the data and the query, record that exemption explicitly so the machine check and the charter agree, and apply the same decision to sql-03-a. 2. Replace option d with a distractor that a specific misreading produces, for example \"East with 1\" (chosen by a candidate who reads HAVING as removing the groups that satisfy the condition), and update the rationale sentence for d accordingly: \"Option d keeps only the group that fails the HAVING condition; HAVING keeps the groups that satisfy it.\""
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Distractor c, \"North with 160 and South with 140\", is eliminated by reading `COUNT(*)` alone and does not probe HAVING at all, whereas in sibling sql-03-a all three distractors model a HAVING misreading (ignore HAVING, apply it per row like WHERE, drop a qualifying group), so sql-03-b gives a candidate shaky on HAVING one tempting wrong option instead of three and is marginally easier than its sibling."
     ],
     "fix": "Keep the same sales table and stem but use an amount-based aggregate so a per-row (WHERE-style) distractor exists, mirroring sql-03-a's distractor roles: query `SELECT region, AVG(amount) AS average FROM sales GROUP BY region HAVING AVG(amount) > 75;` with key \"North with 80 and East with 200\", distractors \"North with 80, South with 70 and East with 200\" (ignores HAVING), \"North with 120, South with 80 and East with 200\" (applies the condition to individual rows before grouping), and \"East with 200\" (drops North, whose average of 80 exceeds 75); update the rationale to explain each alternative accordingly. Alternatively, if COUNT(*) is retained, replace option c with a HAVING-related misreading rather than a SUM/COUNT confusion."
    }
   ]
  },
  {
   "item": "sql-04-b",
   "revised": 0,
   "fails": [
    {
     "lens": "Charter compliance",
     "problems": [
      "Minor: the stem contains two visual elements, a 5-row pipe table of `staff` and a fenced block starting \"```sql\\nSELECT name\\nFROM staff\", which exceeds a strict reading of \"one visual element per item at most\"; note that sibling sql-04-a and every SQL foundations item (sql-01 to sql-06) use the same table-plus-query layout, and the slot blueprint (\"predict the result\" against a shown table) cannot be met without both, so this is a ruling to make for the slot rather than a defect in this item alone.",
      "Minor: the row data uses person-like first names \"Ana\", \"Ben\", \"Cal\", \"Dee\" and \"Eli\" and the options repeat them (\"Ana, Ben, Cal and Eli\"), which touches the \"no person names\" rule; the names are clearly synthetic three-letter placeholders and sibling sql-03-a uses \"Ann\", \"Ben\", \"Cara\" in the same way, so this is a bank-wide convention, not a real-person reference."
     ],
     "fix": "No change to the key, options, rationale or anchor is needed; the item is correct and unambiguous (verified in PGlite: the query returns Ana, Ben, Cal, Eli). Record one ruling for the SQL foundations slots that a shown data table plus its fenced query counts as the single visual of a predict-the-result item, and apply it uniformly to sql-04-a and sql-01 to sql-06. If the owner wants the person-name rule read literally, replace the name values with neutral codes (for example S1 to S5) in the table, the options and the rationale of sql-04-b and sql-03-a together so the slot stays consistent."
    }
   ]
  },
  {
   "item": "sql-05-a",
   "revised": 0,
   "fails": [
    {
     "lens": "Charter compliance",
     "problems": [
      "The stem carries two visual elements, a pipe table (\"| id | assignee |\") and a fenced code block (\"```sql\\nSELECT COUNT(*)\\nFROM tickets\\nWHERE assignee <> 'ana';\\n```\"), whereas the charter allows one visual element per item at most and the SQL slot 2 exemplar puts the query inline in the sentence; minor, and identical in shape to sql-01-a and sql-05-b, so treat it as a bank-wide convention decision rather than a defect unique to this item.",
      "The assignee values \"ana\", \"ben\" and \"cat\" are person first names, and the rules prohibit person names in items; minor, since they are lowercase placeholder data and not identifiable people.",
      "The sourceAnchor \"Intermediate SQL - data filtering: NULL values in comparisons\" names a sub-lesson that does not appear in docs/plan/course-syllabi.md, which lists Chapter 3 Data filtering as basic filtering, multiple conditions and complex filtering; minor, because the course and the chapter topic do exist and sibling sql-01-a anchors to a listed lesson (\"multiple conditions\")."
     ],
     "fix": "In C:/Projects/Test Automation Cap Build/bank/drafts/sql/sql-05-a.json: (1) either accept table-plus-query as the agreed shape for every SQL predict-the-result slot and record that in the review notes, or follow the slot 2 exemplar and write the query inline, e.g. \"What value does SELECT COUNT(*) FROM tickets WHERE assignee <> 'ana' return?\" with the table as the single visual element; (2) replace the assignee values with neutral codes such as 'qa1', 'qa2', 'qa3' (keeping 'qa1' in the WHERE clause and updating the rationale wording \"Only ben and cat\" accordingly); (3) change the sourceAnchor to a listed lesson, e.g. \"Intermediate SQL - data filtering: complex filtering\", and apply the same anchor to sql-05-b so the slot stays consistent."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The assignee values `ana`, `ben` and `cat` in the stem table and the filter `WHERE assignee <> 'ana'` are person first names, which the absolute rules list alongside client, company and product names as content to avoid, whereas the sibling sql-05-b uses neutral numeric values."
     ],
     "fix": "Replace the person names with neutral codes that keep the query and key unchanged, for example assignee values `team_a`, `team_b`, NULL, NULL, `team_c` with `WHERE assignee <> 'team_a'`, and update the rationale wording (\"Only team_b and team_c compare as different from team_a\") and the option a explanation accordingly. Key stays 2, options stay 1, 2, 4, 5 ascending with fixedOrder true."
    }
   ]
  },
  {
   "item": "sql-05-b",
   "revised": 0,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "Key defensibility holds: executing the shown rows in PGlite returns COUNT(*) = 2 and no alternative (3, 4 or 5) is defensible under SQL three-valued logic in any mainstream DBMS.",
      "The stem contains two visual elements, the pipe table \"| id | price |\" and the fenced block \"```sql\\nSELECT COUNT(*)\\nFROM items\\nWHERE price < 20;\\n```\", which exceeds the charter's limit of one visual element per item (the sibling sql-05-a has the same layout, so this is a slot-wide convention)."
     ],
     "fix": "Keep the key and options unchanged. Keep the pipe table as the single visual element and present the query inline in the sentence, for example: \"What value does `SELECT COUNT(*) FROM items WHERE price < 20;` return?\" Apply the same change to sql-05-a so the slot stays interchangeable, or, if the owner confirms that a shown table plus its query counts as one visual element for predict-the-result SQL items, record that decision and leave both items as they are."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "The stem contains two visual elements, a 5-row pipe table (\"| id | price |\") and a fenced code block (\"```sql\\nSELECT COUNT(*)\\nFROM items\\nWHERE price < 20;\\n```\"), which breaks the absolute rule of at most one visual element per item (one code block or one table); severity major because it is an explicit charter rule, and the docs/04 SQL slot 2 exemplar shows the compliant form of a table plus the query written inline in the question sentence.",
      "The sourceAnchor \"Intermediate SQL - data filtering: NULL values in comparisons\" names a sub-topic that does not appear in docs/plan/course-syllabi.md, where Chapter 3 Data filtering lists only \"basic filtering, multiple conditions, complex filtering\"; severity minor because the course and the chapter topic do exist."
     ],
     "fix": "Keep the table as the single visual element and move the query into the question sentence as inline code, so the stem ends: \"What value does `SELECT COUNT(*) FROM items WHERE price < 20;` return?\" (drop the fenced ```sql block). Change the sourceAnchor to a wording drawn from the syllabus, for example \"Intermediate SQL - Chapter 3 Data filtering: basic filtering (NULL behaviour in filters)\", and apply the same anchor wording to sibling sql-05-a for consistency."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "sql-05-b is a template clone of sql-05-a rather than a distinct scenario: both show a 5-row table with exactly two NULL rows, both ask \"What value does this query return?\" for a `SELECT COUNT(*)` with a single comparison, both key to the same value 2, both use the same distractor logic (one option that counts the NULL rows as matching, one option \"5\" that counts every row), and the rationales share the near-verbatim opening \"A comparison with NULL is neither true nor false, so the WHERE clause rejects the two NULL rows\", so the slot has no real variety and its answer is effectively constant across papers.",
      "sql-05-b is likely easier than its sibling because a candidate who does not know three-valued logic still tends to exclude a missing price from \"WHERE price < 20\" by intuition (the missing value does not feel \"below 20\"), whereas sql-05-a's \"WHERE assignee <> 'ana'\" runs against intuition (a missing assignee \"is not ana\"), so a guesser is more likely to land on the key in b than in a."
     ],
     "fix": "Rewrite sql-05-b so it differs from sql-05-a in shape and key while keeping the same NULL-in-comparison rule and difficulty: for example show a 6-row table with two NULL prices and ask which ids are returned by `SELECT id FROM items WHERE price <> 25` (key: the non-NULL rows other than 25, with distractors that include the NULL ids, that include only the 25 row, and that list every row), or keep COUNT(*) but use a table size and operator that yield a key other than 2 and where naive intuition contradicts the key (an inequality such as `<>` on a numeric column). Re-run the final query in PGlite and rewrite the rationale in its own words so it does not mirror sql-05-a."
    }
   ]
  },
  {
   "item": "sql-06-a",
   "revised": 0,
   "fails": [
    {
     "lens": "Charter compliance",
     "problems": [
      "The stem contains two visual elements, a pipe table (\"| id | city |\" with five data rows) and a fenced code block (\"```sql\\nSELECT DISTINCT city\\nFROM offices\\nORDER BY city;\\n```\"), whereas the charter allows \"one visual element per item at most\" and the SQL slot 2 exemplar states the table contents in prose (\"customers(id: 1, 2, 3)\") rather than as a second element; the same pattern appears in the sibling sql-06-b and in every slot 1 to 6 draft, so this is a bank-wide convention rather than an isolated slip, but it is still a deviation from the written rule."
     ],
     "fix": "Keep the fenced ```sql block as the single visual element (the charter requires code in fenced blocks) and replace the pipe table with a prose statement of the data in the exemplar's style, for example: \"The table `offices` has an `id` column and a `city` column. Its five rows hold, in id order, the cities Leeds, York, Leeds, Bath and York.\" Apply the same change to sql-06-b so the two items stay interchangeable, and either apply it across the slot 1 to 6 drafts or record an explicit owner ruling that a shown table plus its query counts as one visual element for predict-the-result SQL items."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The item is a near-clone of sibling sql-06-b: both use a 5-row two-column table with the identical duplicate pattern (rows 1 and 3 repeat, rows 2 and 5 repeat, row 4 is unique) and the identical distractor recipe (first-appearance order, sorted-with-duplicates, reversed direction), so the only substantive differences are the words and \"ORDER BY city;\" versus \"ORDER BY status DESC;\".",
      "The item relies on recall of the default sort direction where the sibling puts the direction on the page: sql-06-a writes \"ORDER BY city;\" and offers \"York, Leeds, Bath\" as a distractor, so a candidate must remember that ORDER BY defaults to ascending, whereas sql-06-b writes \"ORDER BY status DESC\" explicitly, a small asymmetry in what each item asks the candidate to know.",
      "The stem carries two visual elements, the pipe table of offices and the fenced ```sql block, against the one-visual-element rule; the sibling sql-06-b does exactly the same, so interchangeability is unaffected, but the rule tension between \"code in fenced blocks\" and \"one visual element\" should be settled for the slot rather than left to the reviewer."
     ],
     "fix": "Keep the item in the slot but vary its construction so it is a genuine sibling rather than a relabelled copy: change the duplicate pattern (for example one city appearing three times, another once, another once, such as Leeds, Leeds, Bath, Leeds, York) and rebuild the four options from that pattern; write the direction explicitly as \\\"ORDER BY city ASC;\\\" so that, like sql-06-b with DESC, the fact needed is on the page rather than recalled (option d \\\"York, Leeds, Bath\\\" remains a plausible pick for a candidate who confuses ASC with DESC); and, for the slot as a whole, decide once whether the short query sits in a fenced block alongside the table (accepting two visual elements for every predict-the-result SQL item, as the bank already does) or is written inline in the stem sentence as the docs/04 slot 2 exemplar does, and apply the same choice to both slot 6 items."
    }
   ]
  },
  {
   "item": "sql-06-b",
   "revised": 0,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "The stem contains both a pipe table (\"| id | status |\") and a fenced code block (\"```sql\\nSELECT DISTINCT status\"), which is two visual elements against the rule of at most one visual element per item; the key itself is fully defensible (PGlite returns pending, open, closed) and every distractor is refuted by explicit text in the query."
     ],
     "fix": "Replace the pipe table with a prose listing in the style of the SQL slot 2 exemplar, for example: \"The table `tickets` has a `status` column holding these five values, in id order: open, closed, open, pending, closed.\" Keep the fenced SQL block as the single visual element, and apply the same change to sibling sql-06-a so the slot stays interchangeable."
    },
    {
     "lens": "Charter compliance",
     "problems": [
      "The stem contains two visual elements, a five-row pipe table of `tickets` rows and a fenced code block \"```sql\\nSELECT DISTINCT status\\nFROM tickets\\nORDER BY status DESC;\\n```\", which breaks the charter rule \"one visual element per item at most\" (minor; the sibling sql-06-a has the same construction)."
     ],
     "fix": "Keep the table as the single visual element and move the query inline into the question sentence, following the SQL slot 2 exemplar in docs/04: replace the question and fenced block with \"Which values does `SELECT DISTINCT status FROM tickets ORDER BY status DESC;` return, in the order returned?\" Apply the same change to sql-06-a so the two items in the slot stay interchangeable. Everything else checks out: 4 options with exactly 1 correct, ids a to d, section foundations, slot 6 single, difficulty standard, fixedOrder false is appropriate, no negative or opinion wording, no \"Select all that apply.\", distractors plausible and distinct from the key, rationale covers every option, sourceAnchor maps to Intermediate SQL data transformation in docs/plan/course-syllabi.md, and the key (pending, open, closed) was confirmed by running the query against the shown rows in PGlite."
    }
   ]
  },
  {
   "item": "sql-07-a",
   "revised": 0,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Cross-slot near-duplicate: the four options of sql-07-a (\"SELECT id FROM source INTERSECT SELECT id FROM target\", \"SELECT id FROM source UNION SELECT id FROM target\", \"SELECT id FROM source EXCEPT SELECT id FROM target\", \"SELECT id FROM target EXCEPT SELECT id FROM source\") and its source ids 101 to 104 are verbatim the same as sql-08-a, so a paper that draws both serves the same option list twice with only the key changed, and the second item is answerable by elimination, which makes that paper easier than one drawing sql-07-c with sql-08-b. [minor]",
      "Difficulty drift within the slot: sql-07-a is the easiest sibling because its goal phrase \"the ids that appear in both tables\" is a direct gloss of INTERSECT and each distractor is a different operator whose result visibly differs from \"102 and 104\" on the first row, whereas sql-07-b requires the UNION versus UNION ALL distinction (\"listing each id once\") and sql-07-c requires the two-column versus single-column and INNER JOIN distinctions, so a candidate is not equally likely to get each sibling right. [minor]"
     ],
     "fix": "Rewrite sql-07-a so it no longer shares an option list or id values with sql-08-a and so its discrimination is as fine as its siblings: use source ids 301, 302, 302, 303 (one duplicated id) and target ids 302, 304; keep the stem wording pattern but ask \"Which query returns the ids that appear in both tables, listing each id once, giving 302?\"; keep the key \"SELECT id FROM source INTERSECT SELECT id FROM target\" and use distractors \"SELECT s.id FROM source s INNER JOIN target t ON s.id = t.id\" (returns 302 twice), \"SELECT id FROM source UNION SELECT id FROM target\" (301, 302, 303, 304) and \"SELECT id FROM source EXCEPT SELECT id FROM target\" (301, 303); update the rationale to state each distractor's output against the shown rows and verify all four with PGlite before saving."
    }
   ]
  },
  {
   "item": "sql-07-c",
   "revised": 0,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Major: the item drifts into slot 9's territory and cues its neighbours, because its tables (\"| 1 | 100 |\", \"| 2 | 250 |\", \"| 3 | 75 |\" against \"| 1 | 100 |\", \"| 2 | 200 |\", \"| 3 | 75 |\") reproduce sql-09-a's data verbatim for ids 1 to 3, and its key and distractor pair (\"SELECT id, amount FROM source INTERSECT SELECT id, amount FROM target\" versus \"SELECT id, amount FROM source EXCEPT SELECT id, amount FROM target\") is the exact complement of sql-09-b's key and distractor, so a paper serving sql-07-c with sql-09-a or sql-09-b lets one item give away the other (sql-09-a's key shows that a join on id alone ignores amount, eliminating option c here; sql-09-b's key shows that two-column set operations compare whole rows).",
      "Minor: the item is harder and longer than its siblings, because sql-07-a and sql-07-b ask the candidate to pick the single-column set operator matching a goal with all four options being set operations, whereas this item adds two-column row comparison and a join distractor (\"SELECT s.id, s.amount FROM source s INNER JOIN target t ON s.id = t.id\") that requires reasoning about join versus set semantics outside the slot's stated topic, so a candidate who gets a or b right is less likely to get c right."
     ],
     "fix": "Rewrite sql-07-c as a single-column set-operation item with the same shape as its siblings, using data and a goal not used in slots 8 or 9. For example: source ids 301, 302, 303, 304 and target ids 302, 303, 305, 306; stem \\\"Which query returns the ids that appear in target but not in source, giving 305 and 306?\\\"; options: \\\"SELECT id FROM target EXCEPT SELECT id FROM source\\\" (key), \\\"SELECT id FROM source EXCEPT SELECT id FROM target\\\", \\\"SELECT id FROM source INTERSECT SELECT id FROM target\\\", \\\"SELECT id FROM source UNION SELECT id FROM target\\\"; rationale explaining that EXCEPT returns the left set minus the right, the reversed form gives 301 and 304, INTERSECT gives 302 and 303, and UNION gives all six ids; sourceAnchor \\\"SQL for Testers - validating data integrity with set operations (EXCEPT); Joining Data in SQL - set operations\\\". If the two-column INTERSECT is to be kept instead, change the ids and amounts so they no longer match sql-09-a, and replace the INNER JOIN distractor with a set operation such as \\\"SELECT id, amount FROM source UNION SELECT id, amount FROM target\\\" so all four options stay within the slot's topic."
    }
   ]
  },
  {
   "item": "sql-08-a",
   "revised": 0,
   "fails": [
    {
     "lens": "Charter compliance",
     "problems": [
      "Not a charter violation, but a cross-slot cueing risk: sql-08-a and bank/drafts/sql/sql-07-a.json use the same table names `source` and `target`, overlapping ids (101, 102, 103, 104) and the identical four option strings \"SELECT id FROM source EXCEPT SELECT id FROM target\", \"SELECT id FROM source INTERSECT SELECT id FROM target\", \"SELECT id FROM source UNION SELECT id FROM target\" and \"SELECT id FROM target EXCEPT SELECT id FROM source\", so a paper that draws both items lets the candidate use one stem to eliminate options in the other (sql-07-a establishes INTERSECT as the both-tables query, leaving sql-08-a a choice between the two EXCEPT forms)."
     ],
     "fix": "Keep sql-08-a as written (it matches the docs/04 slot 8 exemplar exactly and every query result is verified), and instead differentiate sql-07-a: rename its tables (for example `extract_a` and `extract_b`) and use a different id range so the two items no longer share table names, id values and an identical option set. If sql-07-a must stay as is, change sql-08-a's table names and ids instead (for example `staged` and `published` with ids 301 to 304 and 301, 303) and update the rationale to match."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The stem tables `source.id` 101, 102, 103, 104 with `target`, the four options \"SELECT id FROM source EXCEPT SELECT id FROM target\", \"... INTERSECT ...\", \"... UNION ...\", \"SELECT id FROM target EXCEPT SELECT id FROM source\" and the closing phrase \"giving 102 and 104\" are identical to sql-07-a (which differs only in the target rows and in keying INTERSECT), so a paper drawing both serves two items with the same option list and the same stated output, one leaking the other and making the slot 8 item indistinguishable in form from the slot 7 set-operations item.",
      "Two of the three distractors, \"SELECT id FROM source INTERSECT SELECT id FROM target\" and \"SELECT id FROM source UNION SELECT id FROM target\", are eliminable from the operator name alone, leaving only the reversed EXCEPT as a working distractor, so this item is lighter than sibling sql-08-b, whose LEFT JOIN, INNER JOIN, IS NOT NULL and reversed-join options each need join and NULL reasoning and are roughly twice the length."
     ],
     "fix": "Keep the EXCEPT key but separate the item from sql-07-a and bring its distractor strength level with sql-08-b: rename the tables and ids to match the sibling framing (for example a load job from `expected` into `loaded` with account ids 301 to 305, loaded holding 301, 303, 305, asking for the ids absent from loaded, giving 302 and 304), and replace the name-eliminable UNION option with a plausible wrong query that a candidate must reason about, such as \\\"SELECT id FROM expected WHERE id IN (SELECT id FROM loaded)\\\" (returns the ids that did arrive), keeping INTERSECT and the reversed EXCEPT as the other two distractors. Update the rationale to state the result of each option against the new rows. Alternatively, leave sql-08-a as the exemplar and instead change sql-07-a's table names, id values and stated output so no two items on one paper share an option list and an output phrase."
    }
   ]
  },
  {
   "item": "sql-09-a",
   "revised": 0,
   "fails": [
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The source and target tables in sql-09-a (ids 1 to 3 with amounts 100, 250, 75 and target amount 200 for id 2) are the same rows as sql-07-c's tables, so a paper that draws sql-07-c and sql-09-a shows the candidate near-identical data twice in adjacent applied slots, and the earlier item's rationale-free reading of \"(2, 200)\" carries over into this one rather than each item standing on its own (minor).",
      "Distractor d, \"SELECT s.id FROM source s INNER JOIN target t ON s.amount = t.amount\", is noticeably weaker than the other three and than every distractor in sibling sql-09-b, because joining on the compared value rather than the key is not a shape an unsure tester would reach for, which leaves sql-09-a effectively a three-option item and slightly easier than its sibling (minor)."
     ],
     "fix": "Change the sql-09-a data so it no longer mirrors sql-07-c, for example source amounts 120, 80, 45, 310 and target amounts 120, 95, 45, 300 (still giving ids 2 and 4), and replace distractor d with a plausible wrong shape of the same join-and-compare family that does not produce 2 and 4, for example \\\"SELECT s.id FROM source s INNER JOIN target t ON s.id = t.id AND s.amount = t.amount\\\" (returns 1 and 3) or \\\"SELECT s.id FROM source s LEFT JOIN target t ON s.id = t.id WHERE t.id IS NULL\\\" (returns nothing, the missing-rows pattern); update the rationale to explain the new distractor and re-run all four queries to confirm only option a gives 2 and 4."
    }
   ]
  },
  {
   "item": "sql-09-b",
   "revised": 0,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "The precondition \"every status is present\" is ambiguous: it is meant to rule out NULL statuses (mirroring the sibling's \"every amount is present\"), but for a text column it can be read as \"every status value appears in both tables\", which the shown data contradicts because pending appears only in source; the key is unaffected since all four queries were executed in PGlite and only option a yields (2, closed) and (4, pending)."
     ],
     "fix": "Replace \\\"Both tables hold the same four ids and every status is present.\\\" with \\\"Both tables hold the same four ids and no status is NULL.\\\" (leave options, key and rationale unchanged)."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Minor (calibration): sql-09-b reuses sql-09-a's scenario almost verbatim (\"copies ... from `source` to `target`. Both tables hold the same four ids and every ... is present.\", ids 1 to 4 with rows 2 and 4 differing, and the tail \"giving (2, closed) and (4, pending)\" mirroring \"giving 2 and 4\"), so the two siblings read as one template with the values swapped rather than two independently authored items.",
      "Minor (interchangeability): sql-09-b's key \"SELECT id, status FROM source EXCEPT SELECT id, status FROM target\" and distractors \"SELECT id FROM source EXCEPT SELECT id FROM target\" and \"... INTERSECT ...\" exercise only the set-operation primitives already gated by slots 7 and 8, whereas sibling sql-09-a exercises join-on-id with \"WHERE s.amount <> t.amount\", so a participant strong on joins but weak on set operations is not equally likely to get both siblings right, and a paper drawing sql-09-b tests EXCEPT for a third time while one drawing sql-09-a does not."
     ],
     "fix": "Keep the verified EXCEPT-over-both-columns key but (1) vary the scenario shape away from sql-09-a, for example five rows with ids 1 to 5 where rows 1 and 5 differ, and drop the shared template sentence in favour of a plainly different framing; and (2) replace the id-only EXCEPT distractor (already slot 8 territory) with a join-based wrong form such as \\\"SELECT s.id, s.status FROM source s INNER JOIN target t ON s.id = t.id WHERE s.status = t.status\\\", so that sql-09-b, like sql-09-a, discriminates between the join-and-compare and set-operation approaches rather than only between set operations; re-run all four options in PGlite and update the rationale accordingly."
    }
   ]
  },
  {
   "item": "sql-10-a",
   "revised": 0,
   "fails": [
    {
     "lens": "Charter compliance",
     "problems": [
      "Distractor d, \"DELETE FROM customers;\", is close to absurd filler under the charter's plausibility rule because the stem has just said \"The other three rows are permanent data that must stay\", so a genuinely unsure candidate has no reason to pick a statement with no WHERE clause at all.",
      "The rationale sentence \"Option c uses the reserved id range that only the test rows occupy\" relies on a fact the stem never states (nothing in the item declares 9001 upward to be reserved), so the dispute record justifies option c on grounds a candidate cannot see; it should justify c from the shown rows alone."
     ],
     "fix": "1) Replace option d with a plausible wrong statement that reflects a real confusion, for example \\\"DELETE FROM customers WHERE name = 'TEST%';\\\" (equality with a wildcard matches nothing, so both test rows remain; verified in PGlite that all five rows survive), and update the rationale to say: \\\"Option d uses = rather than LIKE, so the percent sign is a literal character, no name matches and both test rows remain.\\\" 2) Reword the rationale for option c to: \\\"Option c deletes every row whose id is 9001 or higher; in the table shown only the two inserted rows have ids in that range, so ids 1, 2 and 3 remain.\\\" All structural checks (multi, 6 options, 3 correct, ids a-f, section applied, slot 10, one table of 5 data rows, no 'Select all that apply.' in the stem, sourceAnchor naming SQL for Testers and its data generation topic) pass, and PGlite confirms the key set {a, b, c} is the only defensible one."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "Difficulty is below sibling sql-10-b: the whole item is decided by evaluating simple single-column predicates (\"WHERE id IN (9001, 9002)\", \"WHERE id > 2\", \"WHERE city = 'Leeds'\") against five visible rows, which is foundations slot 1 skill, whereas sql-10-b requires reading PRIMARY KEY, UNIQUE and CHECK constraints and recognising a reordered column list, so a candidate is markedly more likely to score on this item than on its sibling under all-or-nothing multi scoring.",
      "Distractor \"DELETE FROM customers;\" is close to absurd filler rather than a plausible wrong answer a genuinely unsure person might pick, which further lowers the item's demand relative to its sibling.",
      "sourceAnchor \"using SQL for data generation: create new entries and delete data; test data hygiene\" names \"create new entries\" although the item exercises only deletion, and \"test data hygiene\" is blueprint wording rather than a lesson title from docs/plan/course-syllabi.md."
     ],
     "fix": "Raise the reading demand to match sql-10-b by replacing the two weakest distractors with compound predicates that must be evaluated against both columns, for example replace option d with \\\"DELETE FROM customers WHERE id >= 9001 AND city = 'Leeds';\\\" (leaves 9002 behind) and option e with \\\"DELETE FROM customers WHERE id >= 9001 OR city = 'Hull';\\\" (also removes permanent id 2), updating the rationale to explain each; keep a, b, c, f as they are. Change sourceAnchor to \\\"SQL for Testers - using SQL for data generation: delete data\\\"."
    }
   ]
  },
  {
   "item": "sql-10-b",
   "revised": 0,
   "fails": [
    {
     "lens": "Key defensibility",
     "problems": [
      "Minor: for option d \"INSERT INTO accounts (id, ref, balance) VALUES (9003, 'TEST-C', -10);\" the stem never states that the database enforces the CHECK constraint, and a candidate familiar with engines that parse but ignore CHECK could argue the row is added; every other aspect of the key (a, e, f succeed; b, c, d fail) was confirmed by executing each statement in PGlite against the table as the script leaves it."
     ],
     "fix": "Add one sentence to the stem after the code block, for example: \"The database enforces every constraint in the script.\" This removes the dialect argument on option d without changing the key (a, e, f)."
    },
    {
     "lens": "Calibration and alignment",
     "problems": [
      "The opening sentence \"An automated test needs rows in `accounts` that it can find again on every run.\" sets up a determinism criterion that plays no part in the key (every option is judged purely on constraint compliance), and \"on every run\" can be read as implying repeated execution of the same INSERT, which would make option a fail on the primary key at the second run; the later clause \"run on its own against the table as the script leaves it\" resolves this, but the framing adds noise that the sibling sql-10-a does not carry.",
      "The key for option d \"INSERT INTO accounts (id, ref, balance) VALUES (9003, 'TEST-C', -10);\" depends on the CHECK constraint being enforced, and the stem does not say which database or that constraints are enforced; a candidate familiar with an engine that historically parsed but ignored CHECK could dispute that d is rejected, which brings engine-specific behaviour into an item that should rest only on facts on the page."
     ],
     "fix": "Replace the first sentence with a plain framing that matches what is tested, for example: \\\"An automated test inserts its own rows into `accounts`. The table was created and populated by this script, and the database enforces every constraint in it.\\\" Keep the final question as \\\"Which of these INSERT statements succeed and add a row, when each statement is run on its own against the table as the script leaves it?\\\" That removes the unused determinism framing, forecloses the repeated-run reading, and states on the page that CHECK is enforced so option d cannot be disputed on engine grounds. No change to options, key or rationale is needed; the key was verified by running all six statements against the DDL in PGlite (a, e, f succeed; b, c, d are rejected)."
    }
   ]
  }
 ]
}